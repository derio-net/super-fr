__pycache__/
*.pyc
.venv/
.coverage
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Claude Code session lockfile (ScheduleWakeup runtime artifact).
.claude/scheduled_tasks.lock

# Local git worktrees.
.worktrees/

# Dormant npm scaffolding. PR #173 removed package.json and
# package-lock.json (nothing reads them since the 2026-04-13
# installer rewrite that switched to .claude-plugin/*.json). Some
# editor / IDE tooling regenerates an empty package-lock.json on
# scan; ignore both names so it doesn't leak into git status.
package.json
package-lock.json
.claude/worktrees/
