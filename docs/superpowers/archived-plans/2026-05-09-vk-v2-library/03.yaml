schema_version: 2
phase:
  number: 3
  title: CLI surface + migration tool + GHA workflow
  tag: agentic
  depends_on:
  - 2
  tracking_issue: null
tasks:
- number: 1
  title: '`vk apply` command + e2e'
  steps:
  - id: P3.T1.S1
    text: |-
      Test e2e — fixture plan → vk apply --dry-run → assert mutations

      Create `tests/integration/test_v2_apply_e2e.py`. Use a tmp git repo with the minimal v2 fixture plan. Invoke `vk apply <plan-dir> --dry-run` via CliRunner. Assert stdout shows the IssueCreate mutation.
      Run: red (no apply command yet).
  - id: P3.T1.S2
    text: |-
      Implement `vk apply` typer command

      Create `src/vk/v2/commands/apply_cmd.py`. Wire into `src/vk/cli.py`:
      ```python
      from vk.v2.commands.apply_cmd import apply_app
      app.add_typer(apply_app, name="apply")
      ```
      Run Step 1: green.
  - id: P3.T1.S3
    text: |-
      Test `vk apply --all` walks plans/

      Run: red.
  - id: P3.T1.S4
    text: |-
      Implement `--all` — find plan folders under `docs/superpowers/plans/`, apply each. Run: green.
- number: 2
  title: '`vk plan create / edit / rework / rework-add / rework-list`'
  steps:
  - id: P3.T2.S1
    text: |-
      Test `vk plan create` scaffolds a folder + appends spec row

      Use a tmp repo with a stub spec containing `## Implementation Plans\n\n| Plan | Repo | File | Depends on |\n|---|---|---|---|`. Invoke `vk plan create --from-spec <spec> --slug test-plan --target-repo derio-net/x`. Assert: folder created with _meta + _prose; spec table has new row.
  - id: P3.T2.S2
    text: |-
      Implement `vk.plan.create()` library function

      Per spec §"Plan editing — vk.plan.*". Atomically: write _meta.yaml, write _prose.md, write empty 01.yaml stub, append row to spec's Implementation Plans table.
  - id: P3.T2.S3
    text: |-
      Implement `vk plan create` CLI command

      Run Step 1: green.
  - id: P3.T2.S4
    text: |-
      Test `vk plan edit --tick P1.T1.S1`

      Tick a step in a fixture plan. Assert state YAML changed; ticked_at populated; idempotent on re-tick.
  - id: P3.T2.S5
    text: |-
      Implement `vk plan edit --tick`

      Library function `vk.plan.tick()`; CLI wrapper. Stages via `git add` after write. Run: green.
  - id: P3.T2.S6
    text: |-
      Test `vk plan edit --complete-phase`

      - Manual phase requires `--note`
      - Agentic phase refuses if any step state == " "
  - id: P3.T2.S7
    text: |-
      Implement `vk.plan.complete_phase()` and CLI

      Run: green.
  - id: P3.T2.S8
    text: |-
      Test `vk plan rework` scaffolds sibling folder + spec row + parent_plan field

      Fixture: a "completed" v2 plan; invoke `vk plan rework <parent>`; assert sibling folder exists with `_meta.parent_plan` set and Implementation Plans table has the rework row.
  - id: P3.T2.S9
    text: |-
      Implement `vk.plan.rework_create()` and CLI

      Per spec §"Rework plans". Includes the cross-directory N-collision check from v1. Run: green.
  - id: P3.T2.S10
    text: |-
      Test `vk plan rework-add`

      Append an origin item; assert `_meta.origin_items` has the new entry with auto-incremented id; track validation rejects bad values.
  - id: P3.T2.S11
    text: |-
      Implement `vk.plan.rework_add_origin()` and CLI

      Run: green.
  - id: P3.T2.S12
    text: |-
      Test `vk plan rework-list`

      Glob plan folders; filter by `_meta.parent_plan`; aggregate status from phase yamls. Assert columns match spec.
  - id: P3.T2.S13
    text: |-
      Implement `vk.plan.rework_list()` and CLI

      Run: green.
  - id: P3.T2.S14
    text: |-
      Test `vk plan self-review` lints

      - Cyclic depends_on → flagged
      - Missing tracking_issue on a phase with state changes → flagged
      - Manual phase with all steps ticked but no completion.note → flagged
  - id: P3.T2.S15
    text: |-
      Implement `vk.plan.self_review()` and CLI

      Run: green.
- number: 3
  title: '`vk pickup`'
  steps:
  - id: P3.T3.S1
    text: |-
      Test `vk pickup` outputs full step text + PR title template + dependency reminder

      Run: red.
  - id: P3.T3.S2
    text: |-
      Implement `vk pickup`

      Per spec — output structured markdown for the agent. Includes the PR title template `[<repo>] <plan-slug> · Phase N/M · <subject>`. Run: green.
- number: 4
  title: '`vk spec status` + `--all`'
  steps:
  - id: P3.T4.S1
    text: |-
      Test `vk.spec.parse()` extracts Implementation Plans table without Status column

      Use a fixture spec file; assert `SpecMeta.plans` has the right PlanRefs.
  - id: P3.T4.S2
    text: |-
      Implement `vk.spec.parse()`

      Create `src/vk/v2/spec.py`. Run: green.
  - id: P3.T4.S3
    text: |-
      Test `vk.spec.compute_status()` aggregates across plans

      Fixture: two plan folders, one Complete, one half-done. Assert SpecStatus shows `1/2 plans complete` and percent calculation.
  - id: P3.T4.S4
    text: |-
      Implement `vk.spec.compute_status()`

      Walk PlanRefs; for each, read plan dir (local fs path-based for now; cross-repo gh API is documented for Plan 3). Aggregate. Run: green.
  - id: P3.T4.S5
    text: |-
      Test `vk.spec.render_status_md()`

      Snapshot test: known input → expected markdown comment body (matches the GHA comment example in the spec).
  - id: P3.T4.S6
    text: |-
      Implement `render_status_md()`

      Run: green.
  - id: P3.T4.S7
    text: |-
      Implement `vk spec status` CLI

      Print rendered markdown.
  - id: P3.T4.S8
    text: |-
      Implement `vk spec status --all`

      Walk `docs/superpowers/specs/`; print one block per spec.
- number: 5
  title: '`vk migrate v1-to-v2`'
  steps:
  - id: P3.T5.S1
    text: |-
      Test migrate converts a sample v1 plan to v2 folder

      - Tmp v1 plan markdown with phases, tasks, steps, tracking comments
      - Invoke `vk migrate v1-to-v2 --dry-run` from the tmp repo
      - Assert stdout shows expected file moves
      - Invoke `vk migrate v1-to-v2 --yes`
      - Assert: folder created with _meta, _prose, 01.yaml; original .md moved to .v1-archive; new folder parses cleanly via `vk.v2.parse`
  - id: P3.T5.S2
    text: |-
      Implement migrate skeleton

      Create `src/vk/v2/migrate.py`. For each .md in plans/ + archived-plans/:
      - Parse via v1 parser
      - Extract per-spec migration mapping
      - Write v2 folder
      - Move .md to .v1-archive
      Run Step 1: green.
  - id: P3.T5.S3
    text: |-
      Test migrate fails loud on per-phase target_repo override

      v1 plan with phases declaring different `**Target repo:**` values → migration fails with clear error.
  - id: P3.T5.S4
    text: |-
      Implement the cross-target-repo loud failure

      Per spec §"Migration" step 2. Run: green.
  - id: P3.T5.S5
    text: |-
      Test migrate --skip-in-progress (default)

      - Tmp repo has two plans: one Status=Complete, one Status=In Progress
      - `vk migrate v1-to-v2 --yes`
      - Assert: Complete migrated; In Progress untouched; warning printed about the skipped plan
  - id: P3.T5.S6
    text: |-
      Implement `--skip-in-progress` (default true)

      Add `--include-in-progress` opt-in flag. Run: green.
  - id: P3.T5.S7
    text: |-
      Test migrate handles v1 rework plans

      Tmp repo with a `*-rework-N.md` v1 file (with Origin table, parent_plan reference). Migrate. Assert: v2 rework folder has `_meta.parent_plan`, `_meta.origin_items` populated from Origin table.
  - id: P3.T5.S8
    text: |-
      Implement v1-rework parsing in migrate

      Reuse existing `src/vk/plan/rework.py:parse_origin_table()`. Run: green.
  - id: P3.T5.S9
    text: |-
      Test migrate updates spec files (drops Status column, points File at folders)

      Tmp spec with Implementation Plans table including Status column. Migrate. Assert: spec table has no Status column; File cells point to folder paths.
  - id: P3.T5.S10
    text: |-
      Implement spec-file rewrite

      Run: green.
- number: 6
  title: GitHub Action workflow file
  steps:
  - id: P3.T6.S1
    text: |-
      Write `.github/workflows/vk-spec-status.yml`

      Reusable workflow using bash/gh CLI (no inline JS), to keep the file simple and reviewable.
      BEGIN: .github/workflows/vk-spec-status.yml
      ```
      name: vk-spec-status
      on:
        workflow_call:
      jobs:
        status:
          if: github.event.pull_request.merged == true
          runs-on: ubuntu-latest
          permissions:
            pull-requests: write
            contents: read
          steps:
            - uses: actions/checkout@v4
              with:
                fetch-depth: 0
            - uses: actions/setup-python@v5
              with:
                python-version: "3.12"
            - name: Install vk
              run: pip install --user "vk @ git+https://github.com/derio-net/superpowers-for-vk@v2.0.0"
            - name: Find specs touched by plan changes
              id: affected
              shell: bash
              env:
                BASE_SHA: ${{ github.event.pull_request.base.sha }}
                HEAD_SHA: ${{ github.event.pull_request.merge_commit_sha }}
              run: |
                set -euo pipefail
                mapfile -t plan_dirs < <(
                  git diff --name-only "$BASE_SHA...$HEAD_SHA" \
                  | grep -E '^docs/superpowers/(plans|archived-plans)/[^/]+/' \
                  | sed -E 's|^(docs/superpowers/(plans\|archived-plans)/[^/]+)/.*|\1|' \
                  | sort -u
                )
                specs=()
                for spec in docs/superpowers/specs/*.md; do
                  [ -f "$spec" ] || continue
                  for d in "${plan_dirs[@]:-}"; do
                    [ -z "$d" ] && continue
                    if grep -q -F "$d" "$spec"; then
                      specs+=("$spec")
                      break
                    fi
                  done
                done
                printf 'specs=%s\n' "${specs[*]:-}" >> "$GITHUB_OUTPUT"
            - name: Build status comment
              if: steps.affected.outputs.specs != ''
              shell: bash
              run: |
                set -euo pipefail
                : > /tmp/status.md
                for spec in ${{ steps.affected.outputs.specs }}; do
                  vk spec status "$spec" >> /tmp/status.md
                  printf '\n\n---\n\n' >> /tmp/status.md
                done
            - name: Post comment
              if: steps.affected.outputs.specs != ''
              shell: bash
              env:
                GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                gh pr comment "${{ github.event.pull_request.number }}" --body-file /tmp/status.md
      ```
      END: .github/workflows/vk-spec-status.yml
  - id: P3.T6.S2
    text: |-
      Add a thin wrapper workflow at `.github/workflows/_pr_spec_status.yml` for this repo's own use

      ```
      name: PR spec status (self)
      on:
        pull_request:
          types: [closed]
      jobs:
        status:
          uses: ./.github/workflows/vk-spec-status.yml
      ```
  - id: P3.T6.S3
    text: |-
      Lint the workflow files

      ```
      command -v actionlint >/dev/null && actionlint .github/workflows/vk-spec-status.yml .github/workflows/_pr_spec_status.yml
      ```
      If actionlint not installed, document and skip.
- number: 7
  title: Final integration
  steps:
  - id: P3.T7.S1
    text: |-
      Full e2e test — fixture plan, dispatch via apply, simulated state changes, complete

      Add `tests/integration/test_v2_full_lifecycle.py`. Use FakeGhClient + tmp git repo.
  - id: P3.T7.S2
    text: |-
      Run full test suite + linters

      ```
      uv run ruff format src/ tests/
      uv run ruff check src/ tests/
      uv run mypy src/vk/v2/
      uv run pytest -q --no-cov
      ```
      All green.
  - id: P3.T7.S3
    text: |-
      Commit Phase 3

        ```
        feat(v2): CLI surface, migration tool, GHA workflow — Phase 3

        All v2 commands operational alongside v1. Migration tool round-trips
        v1 plans to v2 folders (including rework Origin tables and per-phase
        target_repo loud-failure). GHA workflow file lints clean.
        ```

      ---
state:
  steps:
    P3.T1.S1:
      state: x
      ticked_at: null
      note: null
    P3.T1.S2:
      state: x
      ticked_at: null
      note: null
    P3.T1.S3:
      state: x
      ticked_at: null
      note: null
    P3.T1.S4:
      state: x
      ticked_at: null
      note: null
    P3.T2.S1:
      state: x
      ticked_at: null
      note: null
    P3.T2.S2:
      state: x
      ticked_at: null
      note: null
    P3.T2.S3:
      state: x
      ticked_at: null
      note: null
    P3.T2.S4:
      state: x
      ticked_at: null
      note: null
    P3.T2.S5:
      state: x
      ticked_at: null
      note: null
    P3.T2.S6:
      state: x
      ticked_at: null
      note: null
    P3.T2.S7:
      state: x
      ticked_at: null
      note: null
    P3.T2.S8:
      state: x
      ticked_at: null
      note: null
    P3.T2.S9:
      state: x
      ticked_at: null
      note: null
    P3.T2.S10:
      state: x
      ticked_at: null
      note: null
    P3.T2.S11:
      state: x
      ticked_at: null
      note: null
    P3.T2.S12:
      state: x
      ticked_at: null
      note: null
    P3.T2.S13:
      state: x
      ticked_at: null
      note: null
    P3.T2.S14:
      state: x
      ticked_at: null
      note: null
    P3.T2.S15:
      state: x
      ticked_at: null
      note: null
    P3.T3.S1:
      state: x
      ticked_at: null
      note: null
    P3.T3.S2:
      state: x
      ticked_at: null
      note: null
    P3.T4.S1:
      state: x
      ticked_at: null
      note: null
    P3.T4.S2:
      state: x
      ticked_at: null
      note: null
    P3.T4.S3:
      state: x
      ticked_at: null
      note: null
    P3.T4.S4:
      state: x
      ticked_at: null
      note: null
    P3.T4.S5:
      state: x
      ticked_at: null
      note: null
    P3.T4.S6:
      state: x
      ticked_at: null
      note: null
    P3.T4.S7:
      state: x
      ticked_at: null
      note: null
    P3.T4.S8:
      state: x
      ticked_at: null
      note: null
    P3.T5.S1:
      state: x
      ticked_at: null
      note: null
    P3.T5.S2:
      state: x
      ticked_at: null
      note: null
    P3.T5.S3:
      state: x
      ticked_at: null
      note: null
    P3.T5.S4:
      state: x
      ticked_at: null
      note: null
    P3.T5.S5:
      state: x
      ticked_at: null
      note: null
    P3.T5.S6:
      state: x
      ticked_at: null
      note: null
    P3.T5.S7:
      state: x
      ticked_at: null
      note: null
    P3.T5.S8:
      state: x
      ticked_at: null
      note: null
    P3.T5.S9:
      state: x
      ticked_at: null
      note: null
    P3.T5.S10:
      state: x
      ticked_at: null
      note: null
    P3.T6.S1:
      state: x
      ticked_at: null
      note: null
    P3.T6.S2:
      state: x
      ticked_at: null
      note: null
    P3.T6.S3:
      state: x
      ticked_at: null
      note: null
    P3.T7.S1:
      state: x
      ticked_at: null
      note: null
    P3.T7.S2:
      state: x
      ticked_at: null
      note: null
    P3.T7.S3:
      state: x
      ticked_at: null
      note: null
  completion:
    at: '2026-05-12T07:13:16+00:00'
    note: null
    observed_prs: []
