# One-phase plans are first-class

Single agentic phase by design (gh#673 batch): the change is one cohesive gate
change plus its prose, mirrors and one assertion, so splitting it would be the
exact ceremony the fix removes. The plan is unmarked, so until this change lands
the installed gate needs the operator-authorized `skeleton-override-2026-09-26-one-phase-plans`
decision, logged on the spec journal citing this batch.
