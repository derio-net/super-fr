# Journal: 2026-09-09-presentation-showdown

<!-- fr:journal kind=decision scope=plan id=08da34f8334b created=2026-09-09T16:32:29 -->
### 08da34f8334b · decision · Protocol plan with demo-ticket placeholders

4 phases: manual env prep (P1), protocol+instrumentation (P2), dual runs+comparison (P3), Marp deck (P4). Seed prompt and acceptance checklist marked pending operator brief (ticket redacted) during repo prep.

<!-- fr:journal kind=decision scope=plan id=80269b644688 created=2026-09-09T16:34:59 -->
### 80269b644688 · decision · Instrument: runbook + half-1 deck skeleton

experiment/runbook.md (metrics, correction schema, recording runbook; demo-ticket placeholders pending). docs/presentation/super-fr-showdown.md skeleton: half 1 full, half 2 pending recordings, neutral comparison table.

<!-- fr:journal kind=decision scope=plan id=ca0284dfab55 created=2026-09-09T17:00:40 -->
### ca0284dfab55 · decision · Half-1 deck finished ahead of experiment

docs/presentation/super-fr-showdown.md: half 1 complete (12 slides), half 2 preview + empty table. Run stays parked at record-compare pending operator prep.

<!-- fr:journal kind=decision scope=plan id=ab5350f56abb created=2026-09-11T10:04:08 -->
### ab5350f56abb · decision · Half-2 ending left open: comparison vs annotation playback

Operator: likely no time for full comparison. Options: pruned comparison table or annotated session playback. Decision deferred; deck closes open-ended.

<!-- fr:journal kind=decision scope=plan id=7ffa5836d40e created=2026-09-11T10:40:09 -->
### 7ffa5836d40e · decision · Deck rebuilt to factory map: 25 slides, 16 portrait prompts

Stages with ASCII flows, stats+#449, 11 upgrade slides with halo-portrait placeholders, crib+testtrack close, ending open (comparison vs playback). Portrait PNGs pending operator generation.

<!-- fr:journal kind=decision scope=plan id=034fab6daa04 created=2026-09-11T10:42:46 -->
### 034fab6daa04 · decision · Ending locked: annotated fr-goal playback, no full comparison

1h slot. Half 2 = one annotated fr-goal run narrated live. Comparison table dropped.

<!-- fr:journal kind=decision scope=plan id=460b1d3a04a7 created=2026-09-11T18:47:45 -->
### 460b1d3a04a7 · decision · Migrated deck to reveal.js for nonlinear detours

23 horizontal, 12 upgrades carry vertical D-slides with back-links. Marp source retained until reveal verified slide-for-slide. Navigation: Esc overview, S speaker view, hash links.

<!-- fr:journal kind=finding scope=plan id=6d0f93c31412 created=2026-09-16T09:02:02 state=fixed -->
### 6d0f93c31412 · finding [fixed] · data-markdown slides silently dropped all raw HTML

reveal's markdown plugin reads `section.textContent` for inline `data-markdown` slides, so every raw HTML child is reduced to its text. Commit 3f6974b added `data-markdown` around existing markup, which at render time destroyed: 3 crumbs, 2 side portraits, 15 detour back-links (all dead), the `split`/`title`/`divider` slide classes (set via `<!-- .slide: -->` comments, which have no textContent), and 20 speaker notes. The deck still rendered, so nothing looked wrong. Two notes (slides 'What is this talk really about', 'Agenda') were additionally deleted outright by that commit. Fix: wrap every data-markdown body in `<script type="text/template">` (reveal's supported inline form — its textContent is the literal source, so raw HTML survives), convert raw `<aside class="notes">` to reveal's `Note:` separator, flatten a stray wrapper section that orphaned the 'Feature velocity' crumb, restore `class="chain"` on the converted ASCII flow, and restore the 2 deleted notes from c572ec3. Verified by rendering the pre-conversion baseline (c572ec3) and the fixed deck and comparing: 37 leaf slides, 18 crumbs, 0 orphaned, 17 side images, 0 broken, 28 nav links all resolving, 23 non-empty notes, 3 chain blocks, 20 classed slides, identical heading list.

<!-- fr:journal kind=decision scope=plan id=cec37cdd8541 created=2026-09-16T09:53:17 -->
### cec37cdd8541 · decision · Deck prose externalized to slides.md, index.html generated

slides.md is now the source of truth for all 37 slides; `build.py` inlines it into index.html as `<script type="text/template">` blocks (+ template.html for the shell). Chose generation over reveal's external-markdown mode deliberately: external markdown is fetched by XMLHttpRequest, which browsers block under file://, so it would have cost the deck's double-click/offline property. Generating keeps index.html self-contained. Guards: `tests/unit/test_tripwire_deck_fresh.py` (6 tests) fails if index.html is stale, pins the script-template wrapper, and pins that separators inside fenced code do not split slides; `build.py` itself refuses a slide whose `Note:` is not last. That last guard came from a real bug found during the conversion: reveal's notes separator is greedy, so the 15 slides carrying `<img class=\"side\">` after their `<aside>` had the portrait swallowed into the speaker note, where it rendered inside a collapsed `<p>` and `img.side { width: 34% }` resolved to 34% of zero. Verified by fingerprinting the pre-externalization deck and the generated one in a browser and diffing per slide on index, class, background, visible text, note text, side images, images-trapped-in-notes, links and pre classes: 0 diffs across 37 slides, plus all 17 split slides confirmed to have a correctly-sized non-overlapping portrait.

<!-- fr:journal kind=finding scope=plan id=f405276727e8 created=2026-09-16T10:31:20 state=fixed -->
### f405276727e8 · finding [fixed] · Title slide re-laid-out on resize: vh units escape reveal's scaling

Reveal lays slides out on a fixed 960x700 logical canvas scaled by a CSS transform. Lengths in px/em/% live inside that scaled system; `vh`/`vw` are measured against the real window and so do not scale with it. The theme used `vh` in three places, all on the title slide (`padding-top: 6vh` twice, `.topblock { padding: 0 0 3vh }`) plus `img.side { max-height: 76vh }`. Measured drift on the title slide's h1, in logical px: 54 at 1600x900, 37 at 1100x620, while a control split slide held at 88 across both. Fixed by converting to the logical px the `vh` resolved to at fullscreen 1920x1080 (6vh -> 44, 3vh -> 22, 76vh -> 555), preserving the presentation-size appearance to within 0.2px. Verified at four real viewport sizes (1920x1080, 1280x800, 1100x620, 900x1200): title h1 = 44 and h2 = 145 at every one, split controls unchanged. Guard: `test_the_theme_uses_no_viewport_units`, which strips comments first so the explanatory note may still name the units. Note for future measurement work: an in-page resize emulation that sets documentElement width/height cannot detect this class of bug at all, because `vh` tracks the real viewport, which such an emulation never changes.

<!-- fr:journal kind=finding scope=plan id=cea6c61cdeab created=2026-09-16T13:28:07 state=fixed -->
### cea6c61cdeab · finding [fixed] · Title background cropped to 48% on non-16:9 windows

factory-line.png is 1672x941 (aspect 1.777, i.e. 16:9) and the title slide set `data-background-size="cover"`. On a 16:9 screen cover fills exactly, but on anything narrower it scales to the height and crops the sides: 90% of the image width survives at 16:10, and only 47.7% at the operator's portrait 1627x1920 window — cutting off the left-hand station that the slide's left-to-right reading (and its speaker note) depends on. Fixed with `@media (max-aspect-ratio: 16/9)` switching that one background to `contain`. Because the image is itself 16:9, contain and cover agree exactly at 16:9, so there is no visual jump at the switch and the presentation case is byte-for-byte unchanged (verified: 100% x 100% visible at 1920x1080 under both). Selector is `.slide-background.title .slide-background-content` — reveal copies the slide's own class onto its background element, so this needs no :nth-child index coupling; `!important` is required because reveal writes the size as an inline style. Verified at four aspects: 2.37 ultrawide -> cover (unchanged), 1.778 -> contain 100%, 1.60 -> contain 100% (was 90%), 0.85 portrait -> contain 100% (was 47.7%), confirmed visually by screenshot showing all three station plates 1/2/3 where previously only 2 was visible.

<!-- fr:journal kind=decision scope=plan id=a9076d8e2cfd created=2026-09-18T10:22:35 -->
### a9076d8e2cfd · decision · Dual runs reinstated on issue #429; OpenCode --auto + Terra both runs

Reverses 034fab6daa04 (ending locked to an annotated fr-goal playback, comparison dropped): the operator wants both runs recorded after all. Demo feature switched from the redacted board ticket to **super-fr issue #429** (fr acceptance / fr journal can create state but not update it; #431 is its duplicate), which also closes the spec's 'demo brief pending' open item — the brief is now public and quotable on stage. Confirmed unfixed on 4.4.0: journal verbs are add/render/check/handoff, acceptance has no set-status, and journal_cmd.py:89-92 still returns early on a duplicate --id. Rejected as demo features: #472/#438/#435/#467/#471 (change the isolation machinery the run itself stands on), #430 (changes fr-goal itself), #464 (unbounded — 12h/766M tokens), #458 (creates real GitHub issues mid-recording). Harness decided by operator: `opencode --auto`, model github-copilot/gpt-5.6-terra at default effort, BOTH runs. Run B goes first so fr-goal's framing cannot leak into the corrections given to the plain run. Run B is made genuinely plain in three layers: --pure disables the repo's fr-isolation OpenCode plugin, its clone drops .opencode/ + opencode.json (7 instruction files incl. the fr-plan/fr-worktree overrides), and XDG_CONFIG_HOME points at a clean dir so the 25 global skills / 19 commands with the fr-* mirrors do not load; AGENTS.md deliberately stays, being ordinary repo documentation. XDG_DATA_HOME is redirected per run too, which isolates session storage and makes `opencode stats` per-run exact — resolving the spec's 'session-log export path pending' item via `opencode stats` + `opencode export`. Artifacts written: experiment/{runbook,answers,acceptance,corrections}.md and prompts/goal.md. The answer sheet exists to kill the order effect (both runs get identical answers to the design questions); the acceptance checklist is deliberately withheld from both runs, since showing it would hand run B a free specification.

<!-- fr:journal kind=finding scope=plan id=56d0c17ecc59 created=2026-09-18T11:46:46 state=fixed -->
### 56d0c17ecc59 · finding [fixed] · Run B cloned outside ~/Docs/projects committed under the work identity

My runbook said RUN_ROOT=~/experiment-429 'outside any fr-enabled repo'. ~/.gitconfig has [includeIf gitdir:~/Docs/projects/**] -> personal identity, so anything outside that path falls back to the WORK identity: run B's commit ecb868a was authored ioannis.dermitzakis@gebit.de and pushed to a public repo. fr worktrees are unaffected — their gitdir resolves under ~/Docs/projects/super-fr/.git/worktrees, so run A commits as the personal identity. Second trap found in the same place: the keychain https credential for github.com belongs to clawdia-ai-assistant, not the operator, so https remotes act as the wrong account; gh's own protocol is ssh and authenticates correctly. Fixed in the runbook (RUN_ROOT under ~/Docs/projects, ssh clone URL) and salvaged in place for the live run: repo-local identity set, commit re-authored (tree identical, f3ecce1), remote switched to ssh. The branch had in fact pushed successfully, so only gh pr create failed.

<!-- fr:journal kind=decision scope=plan id=d349440fe004 created=2026-09-18T11:46:47 -->
### d349440fe004 · decision · Third arm: vanilla + explicit planning pass

Run B one-shotted #429 with no planning pass and no questions, which would leave fr-goal compared only against an unplanned agent — flattering and uninformative. Added run C: identical clean room and prompt, plus one paragraph instructing a written plan and questions before implementation (experiment/prompts/goal-planned.md). The remaining A-vs-C difference is then what the tooling adds over a well-phrased instruction, which is the honest question the deck asks. Also recorded: subagent accounting is available — opencode 1.18 stores sessions in a per-XDG_DATA_HOME sqlite db whose session table has parent_id, agent, cost, tokens_* and time_created/updated, so subagents are child rows and a run total includes them. Live A: 13 sessions (12 subagents), $6.82, 760k in / 79k out / 17.5M cache-read. Live B: 1 session, $0.84, 2.1M cache-read. Cache-read dominating cost matches #464.

<!-- fr:journal kind=finding scope=plan id=22fbaaf3588a created=2026-09-18T12:36:21 state=fixed -->
### 22fbaaf3588a · finding [fixed] · Correction: the identity bug was gitconfig ordering, not the clone path

Supersedes the cause given in the earlier finding on this run. The includeIf for ~/Docs/projects/** was never the discriminator: ~/.gitconfig carried a global [user] block at line 38, AFTER both includeIf directives at lines 1-4, and git applies config in file order with the last value winning. So the work identity overrode every included identity in every repo, whatever its path. ~/Docs/projects/super-fr only commits as the operator because it carries a repo-local user.email, which its fr worktrees inherit through the shared .git; a fresh clone anywhere, including under ~/Docs/projects, got the work identity. Fixed by moving the [user] block above the includeIf directives (backup at ~/.gitconfig.bak-20260918-123527, comment added explaining the ordering constraint). Verified with throwaway repos in three locations: /tmp -> work (from ~/.gitconfig), ~/Docs/projects -> personal (from ~/.gitconfig-personal), ~/gebit -> work (from ~/.gitconfig-work); existing repos unchanged. Lesson for the runbook: check `git config --show-origin user.email` in a fresh clone before its first commit, since a wrong identity is only visible after it has been pushed.

<!-- fr:journal kind=finding scope=plan id=2ae13674af0e created=2026-09-18T13:01:43 state=open -->
### 2ae13674af0e · finding [open] · fr-goal's only hard gate is unimplementable on OpenCode: run A never asked the batched Q&A

Run A produced spec, plan, run cursor, journal (7 decisions) and a reviewed 2-phase implementation, but its root session contains ONE user message and 75 assistant messages: the operator was never asked anything. Ruled out the prompt: ~/.config/opencode/commands/fr-goal.md expands to exactly 'Use the `fr-goal` skill to handle this request.\n\n$ARGUMENTS', and run A's single user message is byte-for-byte that text plus the brief — so our invocation and /fr-goal are the same thing. The cause is that the skill specifies its gate as 'into ONE AskUserQuestion call (max 4, recommended first)' and marks it a **hard gate**; AskUserQuestion is a Claude Code tool that OpenCode does not have, scripts/sync-opencode.py performs no tool-name translation, and all three mirrors (canonical, .opencode, .hermes) carry the Claude-only name unchanged. With no tool to call the model simply proceeded — zero assistant messages in the session even mention questions. The run cursor shows brainstorm state=done at 09:03:38 with a spec emitted, i.e. a gated step self-resolved. Same exposure in fr-init (AskUserQuestion appears in fr-goal and fr-init, twice each, in all three mirrors). Consequences for the experiment: run A must be described as fr-goal MINUS its single operator touchpoint, not fr-goal as designed; and run C pausing to ask about --note semantics is not C's methodology beating A's, it is C's harness being able to ask at all. Secondary factor to keep in the comparison: #429 is unusually well-specified (it proposes the exact command shapes), so even a working gate would have had little to ask — run C found exactly one genuine question. Also recorded: cfg-A ended up with no skills, commands or instructions (the prepare script's copy did not take); run A worked only because it ran in the base clone, whose repo-level .opencode/skills supplied the fr skills, which is also why /fr-goal was unavailable as a command.

<!-- fr:journal kind=decision scope=plan id=f1b7102a2c28 created=2026-09-18T14:33:39 -->
### f1b7102a2c28 · decision · Three-arm comparison filled from measurement

A=fr-goal #479, B=plain #478, C=plain+plan #480, all on opencode --auto + gpt-5.6-terra from 9bb2248. Cost $7.59 / $1.07 / $0.97; wall 56 / 78 / 105 min; diffs 37f +1826-113 / 18f +345-35 / 21f +467-85 (C measured from its setup commit, else the experiment's own .opencode removal reads as -1721 it never wrote); sessions 14 (13 subagents) / 1 / 1; new tests 12 / 3 / 3; CI fails typecheck / green / fails test; artifacts spec+plan+run+journal / none / none. All three delivered an IDENTICAL CLI surface (journal update, acceptance set-status, add-level, same flags) because answers.md dictated the shapes — the sheet moved the comparison off API taste onto process and quality, as intended. Probed each arm's own build in scratch repos: rows 1-6 and 9 pass for all three (state flip rewrites marker AND heading, duplicate --id exits 2, unknown --id exits 2). Row 8 (down-transition or failing requires --note) FAILS for all three although it is answers.md row 3, given identically to each — and A's spec, plan, per-phase review and self-review all passed over it. Silent divergence found where C asked: --note on acceptance set-status APPENDS in A but REPLACES in B and C, while all three append on the journal side; one asked question would have settled both. Caveats recorded in experiment/comparison.md: A never asked its batched Q&A and could not (#436, AskUserQuestion absent on OpenCode), so A is fr-goal minus its only operator touchpoint and C's 3 operator turns are harness capability not method; #429 is unusually well specified; gh was unauthenticated in all arms so PR-opening scores for nobody; B's and C's clones had the fr instruction mirrors regenerated mid-run by sync-opencode.py.

<!-- fr:journal kind=finding scope=plan id=1b03aa910db2 created=2026-09-18T14:43:14 state=open -->
### 1b03aa910db2 · finding [open] · A's review findings are live bugs in B and C

Probed each arm's own build rather than reading diffs. A's reviewer raised 9 findings and fixed all 9 before delivery; three tested here are present in both other arms. (1) 'fr journal update --scope bogus': A exits 2 with a clean message, B and C both raise an unhandled KeyError traceback. (2) duplicate entry ids: A refuses (exit 2, parse error), B silently updates 1 of 2, C rewrites BOTH entries — exactly the ambiguity A's finding journal-duplicate-id-review describes. (3) acceptance set-status against the repo's REAL matrix.yaml: A preserves all 25 comment lines and appends the note; B corrupts the file — org/repo/rows destroyed, the document parses as a list, and B's own fr acceptance check then rejects the file it just wrote — and also reflows unrelated rows' notes and replaces rather than appends the target note; C refuses a valid file (exit 2) and leaves it untouched, so unusable rather than destructive. Shared root cause for (3): the real matrix has a COMMENT on line 23 containing the literal 'rows:' while the key itself is on line 28; B splits on read_text().split('rows:',1)[0] and C on original.partition('rows:'), so both cut the document inside that comment. A avoids it entirely by editing source spans (yaml.compose + end_mark offsets), the shape its own finding acceptance-row-comments-lost demanded. B's CI is fully green: its tests never fed it the repo's real matrix. Counterweight: checklist row 8 (--note required on a down-transition or failing) is answers.md row 3, given identically to all three, and NO arm implemented it — A's spec, plan, per-phase review and self-review all passed over a stated requirement, so the review loop catches implementation defects but did not catch a dropped requirement.
