# Journal: 2026-09-20-phases-file-tier-reaches-dispatch

<!-- fr:journal kind=discovery scope=plan id=nrb-p5t2 created=2026-09-20T13:36:41 -->
### nrb-p5t2 · discovery · no-refactor-because P5.T2

P5.T2 writes no code — it moves acceptance-matrix row statuses through fr acceptance set-status, runs scripts/bump-version.py, and checks the explainer. There is no implementation to clean up, and the three artifacts it touches (matrix.yaml and the two committed report renderings plus the markdown one) are all generated by fr itself, which forbids hand-editing them. A refactor step here could only be ceremony.

<!-- fr:journal kind=discovery scope=plan id=disc-dogfood created=2026-09-20T13:36:57 -->
### disc-dogfood · discovery · The bug reproduced on this plan's own scaffolding — live evidence, not a fixture

This plan was scaffolded by the very command it fixes. The phases file declared a tier on all five phases (standard/standard/standard/hard/standard). After 'fr plan create --phases-file' at fr 4.8.0: 01.yaml kept skeleton AND acceptance and lost tier; 02-05.yaml kept acceptance and lost tier. Zero tier keys across five phases, exit 0, no warning. The tiers were then restored by hand — the exact workaround gh#434 reports being applied three times in another repo. They were inserted as the LAST header key, where the fixed emitter will write them, so a future re-scaffold of this plan is byte-identical rather than merely equivalent.

<!-- fr:journal kind=decision scope=plan id=d-floor created=2026-09-20T13:36:58 -->
### d-floor · decision · This plan's fr_version floored at >=4.2.0 rather than the default

Self-review warned that the default '>=3.0.0,<5.0.0' admits both a pre-acceptance (3.7.0) and a pre-skeleton (4.2.0) fr while the plan carries both fields. Floored at the highest, '>=4.2.0,<5.0.0', which also clears the tier probe phase 2 adds (tier landed in 3.12.0). Worth recording because it is the same defect class the phase 2 probe exists to catch, observed on this plan before that probe was written — the existing acceptance and skeleton probes caught it, which is the evidence that the tier probe is the missing third of a working set rather than a new idea.

<!-- fr:journal kind=discovery scope=plan id=p1-preexisting-unrelated-failures created=2026-09-20T13:48:01 phase=1 -->
### p1-preexisting-unrelated-failures · discovery · 3 pre-existing test failures, unrelated to phase 1 (phase 1)

uv run pytest -q --no-cov shows 3 failures unrelated to this phase's changes: tests/unit/test_run_workspace.py::test_a_forged_worktree_marker_in_a_plain_directory_is_refused, tests/unit/test_run_workspace.py::test_an_external_marker_without_container_evidence_is_refused, and tests/unit/test_workflow_check.py::test_cli_all_fails_when_nothing_is_discoverable. Confirmed via git stash (reverting phase 1's edits) that all three fail identically on the unmodified worktree — environment-dependent (likely this workspace's own container/worktree evidence, or a discoverable fr-goal workflow shape) rather than caused by the tier fix. Not investigated further; out of phase 1 scope.

<!-- fr:journal kind=finding scope=plan id=p1-f1 created=2026-09-20T13:51:42 phase=1 state=fixed -->
### p1-f1 · finding [fixed] · Byte-stability — an explicit spec goal — had no test at all (phase 1)

The spec's stated contract is 'emitted only when set so pre-tier plans stay byte-stable', and the plan step said to confirm it. Nothing pinned it: grep found no assertion anywhere that a phases file WITHOUT tier produces a header with no tier key. The skeleton precedent (test_plan_acceptance_links.py:186) does exactly this for its own field, so the omission was asymmetric as well as untested. Fixed class-wide rather than for tier alone: the derived test now scaffolds TWO phases — one carrying every optional field, one carrying none — and asserts the bare phase omits every field not in _ALWAYS_EMITTED. A future optional field emitted unconditionally now fails this test. Verified the assertion has teeth by mutation: changing 'if ps.tier is not None' to emit unconditionally makes it fail at line 155, and it passes again on revert.

<!-- fr:journal kind=finding scope=plan id=p1-f2 created=2026-09-20T13:51:43 phase=1 state=fixed -->
### p1-f2 · finding [fixed] · The derived test gave phase 1 depends_on: [1] — a self-dependency (phase 1)

_REPRESENTATIVE_VALUES put every optional field on a single phase numbered 1, including depends_on: [1]. That is phase 1 depending on itself, which violates the plan model's backward-only rule (phase N may only reference phases < N) and is caught by self-review's cycle check, not by create(). It passed only because create() does not validate dependencies — and it sits in precisely the function phase 1 just created, _preflight_phase_error, which is where a cycle check would naturally be added. The day someone adds one, this test breaks with a message about cycles in a test about field coverage. Fixed by moving the optional fields onto phase 2, where depends_on: [1] is valid and actually meaningful.

<!-- fr:journal kind=discovery scope=plan id=p2-blast-radius created=2026-09-20T13:59:19 phase=2 -->
### p2-blast-radius · discovery · Both tier warnings fire on this repo's own plans exactly as D2 predicted, exit code stays 0 (phase 2)

Ran fr plan self-review over every plan in docs/superpowers/plans/. 2026-07-09-multi-backend-git-host-adapters and 2026-09-04-worktree-traceability (both pre-tier, default fr_version floor) now show 8 and 6 untiered-agentic-phase warns respectively, one per agentic phase, plus their pre-existing skeleton-marker error. 2026-09-19-opencode-subagent-dispatch and 2026-09-20-opencode-tier-binding-reaches-dispatch and this plan itself (2026-09-20-phases-file-tier-reaches-dispatch, floored at >=4.2.0 per d-floor) pass clean. Both plans with warns exit 1 -- but confirmed via git stash (reverting phase 2's source) that they ALREADY exited 1 before this phase, from the pre-existing skeleton-marker error alone; the new tier warnings are additive severity=warn and do not change either exit code. No plan's exit code changed as a result of this phase.

<!-- fr:journal kind=discovery scope=plan id=p2-refactor-scope created=2026-09-20T13:59:25 phase=2 -->
### p2-refactor-scope · discovery · The shared _version_floor_issue helper covers acceptance+tier only, not skeleton's 4.1.1 probe (phase 2)

P2.T1.S3's task text names exactly two call sites to extract: _acceptance_link_issues' 3.7.0 probe and _tier_issues' 3.12.0 probe ("the same seven lines twice ... call it from both"). _skeleton_issues' 4.1.1 probe is structurally identical but was left untouched -- refactoring it too would be unrequested scope creep on a task that scoped the extraction explicitly. The helper's docstring says 'and, in principle, any future field's' to signal it's ready for a third caller without claiming skeleton was migrated in this phase.

<!-- fr:journal kind=finding scope=plan id=p2-f1-shared-fixture created=2026-09-20T14:08:05 phase=2 state=fixed -->
### p2-f1-shared-fixture · finding [fixed] · The untiered-agentic-phase warning broke 3 unrelated self-review == [] tests via the shared v2_plan_minimal fixture (phase 2)

tests/unit/fixtures/v2_plan_minimal is the 'clean plan' baseline used by 30+ test files; its phase is agentic and previously carried no tier. Adding the untiered warning made self_review() non-empty for it, breaking test_v2_plan_ops.py::test_self_review_clean_plan_has_no_issues and two tests in test_plan_workflow_binding.py (test_self_review_is_clean_for_a_plan_naming_a_valid_shape, test_self_review_says_nothing_about_a_plan_that_names_no_shape) -- found only by running the FULL suite, not the scoped tier/acceptance test files (the discipline P2.T2.S3's gate exists to enforce). Fixed the same way the skeleton gate's own landing was handled: added 'tier: standard' to the fixture's 01.yaml, between tracking_issue and skeleton per PhaseHeader's field order. Verified no snapshot/golden-string test pins the fixture's exact byte content (grepped for hardcoded 'Fixture phase' text and read_text()== comparisons; all are before/after round-trip checks, not fixed literals) before editing it. Re-ran all 30+ files referencing the fixture (607 passed) plus the full suite.

<!-- fr:journal kind=discovery scope=plan id=p2-matrix-deferred created=2026-09-20T14:12:16 phase=2 -->
### p2-matrix-deferred · discovery · The completion warning about acceptance row plan-self-review-flags-tier-gaps still not-implemented is expected -- deferred to Phase 5 (phase 2)

'fr plan edit --complete-phase 2' printed a warning that its linked acceptance row (plan-self-review-flags-tier-gaps) is still not-implemented. Checked 05.yaml: P5.T2.S2 explicitly flips this row (plus phase-header-fields-survive-phases-file and phase-tier-reaches-the-dispatch-brief) to ci together, citing evidence refs, as part of the plan's designed matrix-bundling. Left it alone in phase 2 rather than preempting phase 5's task.

<!-- fr:journal kind=finding scope=plan id=p2-f2 created=2026-09-20T14:14:30 phase=2 state=fixed -->
### p2-f2 · finding [fixed] · Two of three floor probes extracted, leaving the third claiming to be the same (phase 2)

P2.T1.S3 extracted _version_floor_issue from the acceptance (3.7.0) and tier (3.12.0) probes as the plan asked, but _skeleton_issues' 4.1.1 probe kept its own inline copy — while its comment read 'Same floor probe as acceptance: (#352 review)'. Three instances of one pattern, two extracted, one left, and the one left ASSERTING it is the same: a later reader cannot tell whether skeleton's probe is deliberately different or merely missed. That ambiguity is the cost the extraction was meant to remove. Routed it through the helper too (the comment now says so), dropping 10 lines. Regression net: all 11 skeleton-gate tests plus the 88 tests across test_plan_tier_gates / test_plan_acceptance_links / test_v2_plan_ops pass UNCHANGED — none were edited to fit.

<!-- fr:journal kind=discovery scope=plan id=p3-matrix-warning-expected created=2026-09-20T14:24:52 phase=3 -->
### p3-matrix-warning-expected · discovery · complete-phase 3 warns on the still-not-implemented phase-tier-reaches-the-dispatch-brief row -- expected, deferred to Phase 5 (phase 3)

fr plan edit --complete-phase 3 printed: 'warning: phase 3 completed but its acceptance rows are still not-implemented: phase-tier-reaches-the-dispatch-brief -- flip them ... or record why in notes.' This is expected and by design: spec section F (Matrix) assigns the new round-trip row's not-implemented -> ci flip to Phase 5 Task 2 (P5.T2.S1), confirmed by reading the phase 5 pickup -- 'Row fr-goal-phase-tiering ... Use fr acceptance set-status ... adding the new evidence refs'. Same pattern as p2-matrix-deferred from phase 2. Non-fatal warning, exit code unaffected; phase 3 completed successfully.
