# Journal: 2026-09-21-fr-triage

<!-- fr:journal kind=discovery scope=plan id=6dac56d140b6 created=2026-09-21T19:08:17 phase=3 -->
### 6dac56d140b6 · discovery · no-refactor-because P3.T5 (phase 3)

Status flips via fr acceptance set-status plus the quality gate. No production code is written in this task, so there is nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=eec2c9fa6567 created=2026-09-21T19:08:18 phase=4 -->
### eec2c9fa6567 · discovery · no-refactor-because P4.T1 (phase 4)

The skill is prose, and its refactor is writing-skills' REFACTOR cycle: re-run the baseline scenario WITH the skill and close any loophole it finds. That re-run needs a second agent, which the phase executor cannot spawn, so it happens in this phase's review, not in a task step. Recorded there as skill-green.

<!-- fr:journal kind=discovery scope=plan id=b37977e7cb24 created=2026-09-21T19:08:18 phase=4 -->
### b37977e7cb24 · discovery · no-refactor-because P4.T2 (phase 4)

Runs the two mirror generators and their guards. Generated files are never hand-edited (AGENTS.md), so there is nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=bf9104fdf073 created=2026-09-21T19:08:18 phase=4 -->
### bf9104fdf073 · discovery · no-refactor-because P4.T3 (phase 4)

One table entry in skills_cmd.SKILLS and one AGENTS.md bullet. Nothing structural to clean up.

<!-- fr:journal kind=discovery scope=plan id=cc920bebc761 created=2026-09-21T19:08:19 phase=4 -->
### cc920bebc761 · discovery · no-refactor-because P4.T4 (phase 4)

Version bump through scripts/bump-version.py plus the full gate. No code of its own.

<!-- fr:journal kind=discovery scope=plan id=46169531b87e created=2026-09-21T19:08:28 phase=1 -->
### 46169531b87e · discovery · no-refactor-because P1.T3 (phase 1)

Adds one name to the READ_ONLY_COMMANDS tuple with its argument, updates the pinned test literal, and runs the skeleton's quality gate. A one-token change leaves nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=9236e15d4000 created=2026-09-21T19:08:28 phase=2 -->
### 9236e15d4000 · discovery · no-refactor-because P2.T5 (phase 2)

Status flips via fr acceptance set-status plus the quality gate. No production code is written in this task.

<!-- fr:journal kind=discovery scope=plan id=227fbb293b8f created=2026-09-21T19:08:40 phase=1 -->
### 227fbb293b8f · discovery · no-refactor-because P1.T1 (phase 1)

Captures two gh outputs verbatim and writes a provenance README. Captured fixtures must not be reshaped, and refactoring them would be exactly the construction the capture rule forbids.

<!-- fr:journal kind=discovery scope=plan id=skill-baseline created=2026-09-21T19:12:59 phase=4 -->
### skill-baseline · discovery · RED baseline: an agent triaging WITHOUT fr-triage caught both planted traps — the skill must target what it actually lacked (phase 4)

writing-skills RED run, recorded verbatim in substance and not softened. Scenario: six real super-fr issues, bodies captured with gh (not constructed), two planted traps drawn from real mistakes: #493 quoting a parity.yaml row as absent when main says enforced (the exact claim the hand-built board ranked as live), and #439/#526 as a duplicate pair. Pressure applied: a ten-minute deadline and a waiting operator. Forge writes were forbidden for safety, since the repo is real and public. RESULT: the baseline did NOT fail the traps. It re-read parity.yaml and called #493 fixed; it flagged #439/#526 as one bug; it noticed #438's cited line drifted from :1013 to :1312; it found #463 already fixed on main (test sets HOME to an empty dir); it ranked by cost of failure; it batched #438 with #469 by subsystem; and it wrote forge actions as commands rather than running them. It also found a real out-of-scope bug (recorded separately as discovery bare-marketplace-path). Every claim was re-verified by the orchestrator in the worktree at 089fb51d; all three novel ones hold. CONCLUSION for P4: at six issues, verify-before-claim is not where an agent fails, so the skill must not spend its 120 lines lecturing on it. The failures actually observed are structural: (1) the output lived only in chat, so the next triage starts from zero; (2) there is no notion of what changed since the last triage, which is exactly how the hand-built board silently lacked four issues; (3) nothing re-runnable, so sync means redoing it. The engine closes those. The verification failure that DID happen was the orchestrator's own, at 41-issue scale, trusting #493's quoted YAML, and this six-issue baseline does not reproduce that pressure. The skill therefore keeps ONE compact verification rule, framed around scale and line drift, and treats the baseline's good behaviours (note drift, forge actions as unrun commands, batch by subsystem) as the required shape of a judgement, not as lessons.

<!-- fr:journal kind=discovery scope=plan id=bare-marketplace-path created=2026-09-21T19:13:10 -->
### bare-marketplace-path · discovery · Out of scope, real: local.py:1301-1302 still points users at the retired bare derio-net marketplace

Found by the RED baseline agent, confirmed by the orchestrator at 089fb51d. The IsolationError for a missing executable scripts/validate-plans.sh tells the user to run bash ~/.claude/plugins/marketplaces/derio-net/scripts/install-validator-wrapper.sh. That is the bare org name AGENTS.md records as retired and purged by both installers, so the advice points at a directory that no longer exists. The sibling message at :1281 already uses derio-net--super-fr. It survived the rename because the path is split across two implicitly concatenated string literals (marketplaces/ then derio-net/scripts/...), so no whole-path grep can match it. The orchestrator's own first grep missed it the same way. Not fixed in this PR: out of scope for fr triage. Recorded as a discovery rather than an open finding because fr journal has no state for true but out of scope (#491), and an open finding would block this run's delivery over work it never claimed. Recommend a one-line fix and an issue.

<!-- fr:journal kind=discovery scope=plan id=c5e5188ad9e1 created=2026-09-21T19:19:45 phase=1 -->
### c5e5188ad9e1 · discovery · artifact-versioning rule prose still says 'the read-only five' (phase 1)

The artifact-versioning rule's CLI-entry-gate section enumerates the exempt commands as 'migrate plus the read-only five — status, skills, isolation, init, validate'. That was already stale before this phase (harness joined in 2026-09-18) and is now two short with triage. The code and its pinned test (test_the_exemption_list_is_exactly_these_things) are the authority and are correct; only the prose lags. Not edited in phase 1 (out of plan scope); a one-line prose fix is owed before delivery.

<!-- fr:journal kind=finding scope=plan id=r-p1-exempt-prose created=2026-09-21T19:22:33 phase=1 state=fixed -->
### r-p1-exempt-prose · finding [fixed] · artifact-versioning.md named 'the read-only five' for a list that is now seven (phase 1)

Raised by the phase-1 executor as discovery c5e5188ad9e1, in scope because this PR is what makes the list seven. The rule's Exempt commands paragraph counted five and named them; harness had already made it six unnoticed, and triage made seven. FIXED structurally rather than recounted: the prose now names fr.artifacts.trigger.READ_ONLY_COMMANDS as the source of truth and states the membership criterion, and new test test_the_rule_prose_names_every_read_only_command asserts the paragraph names every member of the tuple. Mutation-checked: against the old prose it fails naming exactly ['harness', 'triage'], which also proves the pre-existing harness drift; against the new prose it passes. Both mirror generators re-run; .opencode/instructions/artifact-versioning.md regenerated, and .hermes unchanged because SOUL.d carries only plugin rules.

<!-- fr:journal kind=finding scope=plan id=r-p1-test-split created=2026-09-21T19:24:33 phase=1 state=fixed -->
### r-p1-test-split · finding [fixed] · The orchestrator's prose tripwire was inserted INSIDE the pinned exemption test, stealing its EXEMPTIONS ordering assert (phase 1)

Raised by the phase-1 code review (Important #2); verified true. test_the_rule_prose_names_every_read_only_command was inserted after SKIP_ENV_VAR, but the pinned test test_the_exemption_list_is_exactly_these_things continued past that line with assert trigger.EXEMPTIONS == (...). The new def cut the function short, so that ordering assert silently became the tail of the new test. Nothing went red because the moved assert is still true, just in the wrong function, and the orchestrator's own mutation check could not see it for the same reason. FIXED: the assert moved back. Verified structurally with the AST rather than by a passing run, because a passing run is exactly what hid it: the pinned test now owns 4 asserts (EXEMPTIONS, EXEMPT_COMMANDS, EXEMPT_OPTIONS, SKIP_ENV_VAR) and the new test owns 1 (READ_ONLY_COMMANDS). 54 passed.

<!-- fr:journal kind=finding scope=plan id=r-p1-org-abort created=2026-09-21T19:24:34 phase=2 state=open -->
### r-p1-org-abort · finding [open] · --org aborts the whole run on the first unreadable repo (known interim state until P2.T4) (phase 2)

Review Important #1, verified: collect_facts loops the org's repos, and the first GhError from any repo's list_issues propagates to exit 2 with no facts.json, contradicting spec 3.C (one unreadable repo never aborts the board). Accepted knowingly as interim rather than gating --org off, which phase 2 would only have to undo. P2.T4.S1's RED test (the fake raises on repo 2; expect skipped) is confirmed to start red against this code, so the gap is closed by construction. Filed against phase 2 so it gates that review.

<!-- fr:journal kind=finding scope=plan id=r-p1-gherror-leak created=2026-09-21T19:24:34 phase=2 state=open -->
### r-p1-gherror-leak · finding [open] · fr.gh.GhError is imported by the command layer, so the seam leaks one exception type (phase 2)

Review Minor #1, verified: triage_cmd.py imports and catches fr.gh.GhError, so the command layer knows gh exists, against decision d2. P2.T4.S3 plans a triage-owned forge error raised by GhForge. Phase 2 must REPLACE this import with the triage error, not add a second catch beside it.

<!-- fr:journal kind=finding scope=plan id=r-p1-gh-missing created=2026-09-21T19:24:35 phase=2 state=open -->
### r-p1-gh-missing · finding [open] · A missing or unauthenticated gh binary gives an uncaught FileNotFoundError traceback (phase 2)

Review Minor #2, verified: fr.gh._run_gh catches only CalledProcessError, so an absent gh binary raises FileNotFoundError out of collect. This is pre-existing in fr.gh, but triage is the first verb commonly run from outside any repo, where a missing gh is likelier. Phase 2's GhForge must map it to the triage forge error with a one-line message naming the fix.

<!-- fr:journal kind=finding scope=plan id=r-p1-repo-cap created=2026-09-21T19:24:35 phase=2 state=open -->
### r-p1-repo-cap · finding [open] · fr.gh.list_repos hardcodes --limit 200, so an org past 200 repos would be silently truncated (phase 2)

Review Minor #3, verified at gh.py list_repos. This is the same rows-silently-absent failure spec 3.C guards against for issues, one level up. The spec is now amended (3.C): the repo list gets the same possibly-truncated warning when it returns exactly its limit. Phase 2 implements and tests it.

<!-- fr:journal kind=finding scope=plan id=r-p1-state-dup created=2026-09-21T19:24:35 phase=2 state=open -->
### r-p1-state-dup · finding [open] · Phase 1 already has Scope.name and default_state_dir; phase 2 must extend them, not write a second copy (phase 2)

Review Minor #4, verified: collect.py holds Scope (with .name producing owner--repo / owner) and default_state_dir built on _home(). P2.T1.S2 asks for state_dir(scope, override) in model.py, and P2.T1.S3 for one scope-naming function. Phase 2 must MOVE or extend the phase-1 code into that single home. Two scope-naming functions are exactly what P2.T1.S3 exists to prevent.

<!-- fr:journal kind=finding scope=plan id=r-p1-untested created=2026-09-21T19:24:36 phase=2 state=open -->
### r-p1-untested · finding [open] · The usage-error and forge-error CLI paths are untested, including r7 escaping on an error string (phase 2)

Review Minor #5, verified: no test covers _scope's exit-2 usage errors (neither or both of --repo/--org) or the forge-error path. Phase 2 adds CliRunner cases for both, and the forge-error case must use an error string containing [/red] so r7's escaping is pinned on errors as well as on titles.

<!-- fr:journal kind=finding scope=plan id=r-p1-view-fields created=2026-09-21T19:24:36 phase=2 state=open -->
### r-p1-view-fields · finding [open] · fr.gh.view_issue returns only title, body, labels, state — too little for a judged-but-closed row (phase 2)

Review Minor #6, verified at gh.py view_issue. The judged-but-closed path (P2.T4) needs at least number, url and closedAt to render and link the row. view_issue's --json list is shared with other callers, so ADD fields rather than change existing ones, or derive number and url in GhForge from the key.

<!-- fr:journal kind=finding scope=plan id=r-p1-pr-window created=2026-09-21T19:24:37 phase=2 state=fixed -->
### r-p1-pr-window · finding [fixed] · super-fr's own PR list returned exactly the 200-PR limit at capture, so the board's first real run will warn (phase 2)

From the review's closing recommendation, verified against tests/fixtures/triage/README.md. The spec is now amended (section 4 Risks): the possibly-truncated warning firing on this repo is correct behaviour, and --pr-limit widens the window. Recorded as fixed because the owed change was the spec acknowledging it.

<!-- fr:journal kind=review scope=plan id=review-p1 created=2026-09-21T19:24:50 phase=1 -->
### review-p1 · review · Phase 1 review: ready with fixes — seam, limits, state dir, gate exemption and fixtures all hold (phase 1)

superpowers:requesting-code-review over spec 3.A-F', plan 01/02.yaml and d0e426eb..e3c58bc8, then superpowers:receiving-code-review with every finding verified against the code before acting. Held without change: the Forge seam (collect.py is the only fr.gh import in fr.triage), explicit --limit 1000/200 with field lists matching 3.C exactly, the HOME-based state dir with no XDG (r1), the gate exemption meeting its list's own criterion (writes only facts.json under the state dir), and fixtures that are genuinely captured (real GraphQL node ids, correct reference nesting, bodies 519-7136 chars exercising truncation, 15 closed referenced issues giving phase 2 a real judged-but-closed case). Raised: 1 Important and 6 Minor from the reviewer, plus the orchestrator's own r-p1-exempt-prose. FIXED in phase 1: r-p1-exempt-prose; r-p1-test-split, the orchestrator's own regression, caught by the reviewer and verified by AST rather than by a passing run; r-p1-pr-window, fixed as a spec amendment. FILED AGAINST PHASE 2, gating its review: r-p1-org-abort, r-p1-gherror-leak, r-p1-gh-missing, r-p1-repo-cap (spec amended), r-p1-state-dup, r-p1-untested, r-p1-view-fields. None refuted: every finding checked out.

<!-- fr:journal kind=finding scope=plan id=r-p1-state-dup-resolved created=2026-09-21T19:28:16 state=fixed resolves=r-p1-state-dup -->
### r-p1-state-dup-resolved · finding [fixed] · resolves r-p1-state-dup: Phase 1 already has Scope.name and default_state_dir; phase 2 must extend them, not write a second copy

P2.T1 (2fdd5093) MOVED Scope (with .name) and the state dir out of collect.py into fr.triage.model; default_state_dir is gone, replaced by the single state_dir(scope, override) used by triage_cmd. No second scope-naming function exists. Pinned by tests/unit/test_triage_model.py::test_default_state_dir_for_a_repo_is_under_home_cache, ::test_default_state_dir_for_an_org_is_under_home_cache, ::test_dir_overrides_the_default_for_both_scopes.

<!-- fr:journal kind=finding scope=plan id=r-p1-org-abort-resolved created=2026-09-21T19:34:37 state=fixed resolves=r-p1-org-abort -->
### r-p1-org-abort-resolved · finding [fixed] · resolves r-p1-org-abort: --org aborts the whole run on the first unreadable repo (known interim state until P2.T4)

d402d15a: collect_facts catches ForgeError per repo in org scope and records Skipped(repo, reason); the rest still collect. RED confirmed against the prior code (ForgeError 'issues are disabled...' propagated). Tests: tests/unit/test_triage_collect.py::test_a_repo_whose_issue_list_fails_is_skipped_and_the_rest_collect, ::test_a_repo_whose_pr_list_fails_is_skipped_too; CLI: tests/unit/test_triage_cli.py::test_org_scope_reports_a_skipped_repo_verbatim_and_still_writes. Repo scope still errors (::test_in_repo_scope_a_failing_repo_is_an_error_not_an_empty_board).

<!-- fr:journal kind=finding scope=plan id=r-p1-gherror-leak-resolved created=2026-09-21T19:34:38 state=fixed resolves=r-p1-gherror-leak -->
### r-p1-gherror-leak-resolved · finding [fixed] · resolves r-p1-gherror-leak: fr.gh.GhError is imported by the command layer, so the seam leaks one exception type

d402d15a: the GhError import and catch in triage_cmd.py were REPLACED (not supplemented) by one catch of fr.triage.errors.TriageError, whose subclass ForgeError is what GhForge raises. Pinned structurally by tests/unit/test_triage_collect.py::test_fr_triage_touches_gh_only_in_collect (no module in fr.triage or triage_cmd but collect.py mentions fr.gh/GhError) and ::test_gh_forge_raises_the_triage_forge_error_not_gh_error.

<!-- fr:journal kind=finding scope=plan id=r-p1-gh-missing-resolved created=2026-09-21T19:34:38 state=fixed resolves=r-p1-gh-missing -->
### r-p1-gh-missing-resolved · finding [fixed] · resolves r-p1-gh-missing: A missing or unauthenticated gh binary gives an uncaught FileNotFoundError traceback

d402d15a: GhForge wraps every call in _forge_errors(), mapping FileNotFoundError to ForgeError(GH_MISSING), a one-line message naming https://cli.github.com and gh auth login. Test runs with PATH set to an empty tmp dir (no subprocess patching): tests/unit/test_triage_collect.py::test_gh_forge_with_no_gh_binary_raises_a_one_line_forge_error.

<!-- fr:journal kind=finding scope=plan id=r-p1-repo-cap-resolved created=2026-09-21T19:34:39 state=fixed resolves=r-p1-repo-cap -->
### r-p1-repo-cap-resolved · finding [fixed] · resolves r-p1-repo-cap: fr.gh.list_repos hardcodes --limit 200, so an org past 200 repos would be silently truncated

d402d15a: fr.gh.list_repos gains limit (default 200, unchanged) and include_archived (default False, unchanged); GhForge asks with include_archived=True so scope_repos counts the RAW list against REPO_LIMIT before dropping archived repos, and records Truncation(source=repos). Tests: tests/unit/test_triage_collect.py::test_a_repo_list_returning_exactly_its_limit_warns_even_with_archived_repos, ::test_the_repo_list_is_asked_for_its_limit_and_archived_repos_are_dropped, ::test_gh_forge_asks_gh_for_archived_repos_too_so_the_limit_is_countable; tests/unit/test_gh.py::TestListRepos::test_limit_and_archived_are_selectable.

<!-- fr:journal kind=finding scope=plan id=r-p1-untested-resolved created=2026-09-21T19:34:39 state=fixed resolves=r-p1-untested -->
### r-p1-untested-resolved · finding [fixed] · resolves r-p1-untested: The usage-error and forge-error CLI paths are untested, including r7 escaping on an error string

d402d15a: tests/unit/test_triage_cli.py::test_not_exactly_one_of_repo_or_org_is_a_usage_error[neither|both] (exit 2, no facts.json) and ::test_a_forge_error_exits_2_and_prints_the_message_verbatim with the error string '[manual] boom [/red] tail' printed verbatim (no MarkupError, prefix kept). Skipped reasons are escaped too: ::test_org_scope_reports_a_skipped_repo_verbatim_and_still_writes.

<!-- fr:journal kind=finding scope=plan id=r-p1-view-fields-resolved created=2026-09-21T19:34:39 state=fixed resolves=r-p1-view-fields -->
### r-p1-view-fields-resolved · finding [fixed] · resolves r-p1-view-fields: fr.gh.view_issue returns only title, body, labels, state — too little for a judged-but-closed row

d402d15a: fr.gh.view_issue's --json list is now ISSUE_VIEW_FIELDS = number,title,body,labels,state,url,closedAt, only ADDING fields (the original four stay). Tests: tests/unit/test_gh.py::TestViewIssue::test_asks_for_the_fields_a_closed_triage_row_needs; the judged-but-closed row carries closed_at and url in tests/unit/test_triage_collect.py::test_a_judged_issue_no_longer_open_is_viewed_and_appears_closed.

<!-- fr:journal kind=discovery scope=plan id=p2-collect-decisions created=2026-09-21T19:34:59 phase=2 -->
### p2-collect-decisions · discovery · Phase 2 collect: four decisions beyond the plan's letter (phase 2)

1. Forge.list_repos(owner, limit) returns archived repos too, and collect.scope_repos drops them. The plan said GhForge delegates to fr.gh.list_repos 'which already drops archived repos', but counting AFTER that filter would miss a full 200-repo list containing archived repos, defeating r-p1-repo-cap. fr.gh.list_repos gained backward-compatible limit/include_archived kwargs instead. 2. --pr-limit was added to collect (spec 3.C names it and the truncation warning points at it; no plan step did). 3. In REPO scope a forge failure on the one repo is still exit 2, not an empty board with one skipped entry: spec 3.C's 'one unreadable repo never aborts the board' is about org scope, and an empty facts.json would look like a clean backlog. 4. Stage is a derived Issue.stage property, never written into facts.json, so no stored value can disagree with derive_stage. Also: a judgement whose view_issue fails (deleted issue) is simply left out, so phase 3's check reports it orphaned. The GhError swap in triage_cmd (P2.T4.S3) landed in the same edit as P2.T4.S2's GREEN because the r-p1-untested RED test drives the command through a ForgeError.

<!-- fr:journal kind=finding scope=plan id=r-p2-unviewed created=2026-09-21T19:45:59 phase=2 state=open -->
### r-p2-unviewed · finding [open] · A failed view_issue is silently dropped, so check would call a live judgement orphaned (phase 2)

Review Important 1, verified at collect.py:237-238: except ForgeError: continue, under a comment that says deleted or unreadable. A rate limit, 5xx or unreadable token becomes orphaned, and the skill may prune a live judgement: a failure reported as a clean result. Fix NOW, while schema 1 is unreleased: add Facts.unviewed (key + reason) and record every view_issue failure there. Spec 3.C and 3.F are amended.

<!-- fr:journal kind=finding scope=plan id=r-p2-case created=2026-09-21T19:45:59 phase=2 state=open -->
### r-p2-case · finding [open] · Judgement keys and scope names are case-sensitive where GitHub is not, which duplicates issues and orphans rankings (phase 2)

Review Important 2, verified: collect.py:180 set(judged) - open_keys is case-sensitive, while by_name at :178 lowercases, so judged Repo#5 against open repo#5 appears twice (reviewer's repro). And Scope.name keeps the typed case, so --repo Derio-Net/Super-FR names a different state dir and orphans every judgement. Fix: lowercase every key at ONE point (construction and load), lowercase Scope.name, refuse two judgement keys differing only by case as a conflict, and export the normaliser so check and render reuse it. Spec 3.D is amended. Regression test for the duplicate.

<!-- fr:journal kind=finding scope=plan id=r-p2-seam-ast created=2026-09-21T19:46:00 phase=2 state=open -->
### r-p2-seam-ast · finding [open] · The seam test is a source substring match: aliased imports and direct subprocess calls slip past it (phase 2)

Review Minor 3, verified: test_fr_triage_touches_gh_only_in_collect substring-matches source, so from fr import labels, gh or from fr import (gh) or a subprocess.run(['gh', ...]) in a later module bypass it, and only top-level files are scanned. Replace with an AST walk over rglob('*.py'): forbid Import of fr.gh, ImportFrom module fr with alias gh, ImportFrom fr.gh, and any subprocess import, outside collect.py. Prove non-vacuity with a planted violation.

<!-- fr:journal kind=finding scope=plan id=r-p2-blocked created=2026-09-21T19:46:00 phase=2 state=open -->
### r-p2-blocked · finding [open] · No test pins a blocked issue whose only PR was closed unmerged (phase 2)

Review Minor 4. derive_stage returns blocked there, which is right: an abandoned PR advances nothing. The spec said no PR; it is amended to no open or merged PR (3.E). Add the case (blocked label, one CLOSED unmerged PR) -> blocked.

<!-- fr:journal kind=finding scope=plan id=r-p2-schema-strict created=2026-09-21T19:46:01 phase=2 state=open -->
### r-p2-schema-strict · finding [open] · schema: true and schema: 1.0 load, because True == 1 == 1.0 and pydantic's Literal[1] accepts them (phase 2)

Review Minor 5, reviewer confirmed schema: true loads. Check type(value) is int and value == SCHEMA before pydantic sees it, for both files, with tests for true and 1.0.

<!-- fr:journal kind=finding scope=plan id=r-p2-empty created=2026-09-21T19:46:01 phase=2 state=open -->
### r-p2-empty · finding [open] · Org scope with every repo skipped exits 0 and writes an empty board that looks like a clean backlog (phase 2)

Review Minor 6: the same risk that justified exit 2 for repo scope (deviation 3) applies when a token can list repos but read none. Exit 2 with the skipped reasons when nothing was collected. Spec 3.C now states both cases.

<!-- fr:journal kind=finding scope=plan id=r-p2-repos-doc created=2026-09-21T19:46:01 phase=2 state=open -->
### r-p2-repos-doc · finding [open] · Facts.repos includes skipped repos, and phase 3 might present its length as repos collected (phase 2)

Review Minor 7. Defensible as the scope, but ambiguous. Document it in the model's docstring, and expose a collected property (repos minus skipped) so render cannot get it wrong.

<!-- fr:journal kind=finding scope=plan id=r-p2-pattern-ids created=2026-09-21T19:46:02 phase=2 state=open -->
### r-p2-pattern-ids · finding [open] · Judgements.patterns[].ids are never validated, so a typo'd id is silent (phase 2)

Review Minor 8: validate each id against KEY_RE on load, through the same case normaliser as r-p2-case.

<!-- fr:journal kind=finding scope=plan id=r-p2-type-nit created=2026-09-21T19:46:02 phase=2 state=open -->
### r-p2-type-nit · finding [open] · _issue(state: str) needs a type: ignore; type it as IssueState (phase 2)

Review Minor 9, collect.py:337. Cosmetic: remove the ignore by typing the parameter.

<!-- fr:journal kind=finding scope=plan id=r-p2-check-sets created=2026-09-21T19:46:03 phase=3 state=open -->
### r-p2-check-sets · finding [open] · check must report unreachable (unviewed or skipped-repo) judgements separately from orphaned, via the shared key normaliser (phase 3)

From the phase-2 review's section on what makes phase 3 harder. _judged_elsewhere skips repos that were not collected, so a judgement in a skipped repo looks orphaned unless check consults facts.skipped, and r-p2-unviewed adds facts.unviewed. check reports a fourth set, unreachable (spec 3.F, amended), and must compare keys through the r-p2-case normaliser.

<!-- fr:journal kind=finding scope=plan id=r-p2-render-model created=2026-09-21T19:46:03 phase=3 state=open -->
### r-p2-render-model · finding [open] · render must build from the Facts model, never raw JSON, because stage is derived and not on disk (phase 3)

From the phase-2 review, deviation 4: stage is the derived Issue.stage property and is not written to facts.json. A renderer reading raw JSON would find no stage. Load through fr.triage.model and read .stage.

<!-- fr:journal kind=finding scope=plan id=r-p2-case-resolved created=2026-09-21T19:57:17 state=fixed resolves=r-p2-case -->
### r-p2-case-resolved · finding [fixed] · resolves r-p2-case: Judgement keys and scope names are case-sensitive where GitHub is not, which duplicates issues and orphans rankings

One exported normalize_key in fr.triage.model (lowercase canonical), applied by issue_key on construction and by Judgements on load; Scope.name lowercased; collect normalises judged keys; two keys differing only by case refused as a conflict. RED then GREEN: test_triage_collect::test_a_judged_key_differing_only_by_case_is_not_held_twice (reviewer's repro: o/repo, open #5, judged Repo#5 held once, no view), test_triage_model::test_two_judgement_keys_differing_only_by_case_are_a_conflict, test_judgement_keys_are_lowercased_on_load, test_scope_name_is_lowercase_so_typed_case_names_one_state_dir, test_issue_key_is_constructed_lowercase, test_normalize_key_lowercases_a_judgement_key. Commit 829c1acf.

<!-- fr:journal kind=finding scope=plan id=r-p2-unviewed-resolved created=2026-09-21T19:57:17 state=fixed resolves=r-p2-unviewed -->
### r-p2-unviewed-resolved · finding [fixed] · resolves r-p2-unviewed: A failed view_issue is silently dropped, so check would call a live judgement orphaned

Facts.unviewed (Unviewed: normalised key + reason); every view_issue ForgeError is recorded instead of continue; collect prints each (escaped). schema stays 1. RED then GREEN: test_triage_collect::test_every_failed_view_is_recorded_as_unviewed_never_dropped[not-found, HTTP 502, rate limit], test_unviewed_round_trips_through_facts_json, test_triage_cli::test_an_unviewed_judgement_is_reported_verbatim_and_still_writes. Commit 7ce5edd5.

<!-- fr:journal kind=finding scope=plan id=r-p2-empty-resolved created=2026-09-21T19:57:18 state=fixed resolves=r-p2-empty -->
### r-p2-empty-resolved · finding [fixed] · resolves r-p2-empty: Org scope with every repo skipped exits 0 and writes an empty board that looks like a clean backlog

collect_facts raises ForgeError naming every skipped repo and reason when org scope collected nothing (also when the org has no non-archived repos at all); the command maps it to exit 2 and writes no facts.json. RED then GREEN: test_triage_collect::test_org_scope_with_every_repo_skipped_is_an_error_naming_each_reason, test_org_scope_with_no_repos_at_all_is_an_error_not_a_clean_board, test_triage_cli::test_org_scope_with_every_repo_skipped_exits_2_with_the_reasons. Commit efdaa459.

<!-- fr:journal kind=finding scope=plan id=r-p2-pattern-ids-resolved created=2026-09-21T19:57:18 state=fixed resolves=r-p2-pattern-ids -->
### r-p2-pattern-ids-resolved · finding [fixed] · resolves r-p2-pattern-ids: Judgements.patterns[].ids are never validated, so a typo'd id is silent

Pattern.ids validated against KEY_RE (shared _bad_keys) and normalised through normalize_key on load. RED then GREEN: test_triage_model::test_a_typod_pattern_id_is_refused_naming_the_file (4 typos), test_pattern_ids_go_through_the_same_normaliser_as_keys. Commit bbf78051.

<!-- fr:journal kind=finding scope=plan id=r-p2-schema-strict-resolved created=2026-09-21T19:57:19 state=fixed resolves=r-p2-schema-strict -->
### r-p2-schema-strict-resolved · finding [fixed] · resolves r-p2-schema-strict: schema: true and schema: 1.0 load, because True == 1 == 1.0 and pydantic's Literal[1] accepts them

_check_schema requires type(value) is int and value == SCHEMA before pydantic, for both files. RED then GREEN: test_triage_model::test_a_schema_equal_to_1_but_not_the_int_1_is_refused_in_judgements[true,1.0] and ..._in_facts[True,1.0]. Commit f44467a9.

<!-- fr:journal kind=finding scope=plan id=r-p2-blocked-resolved created=2026-09-21T19:57:19 state=fixed resolves=r-p2-blocked -->
### r-p2-blocked-resolved · finding [fixed] · resolves r-p2-blocked: No test pins a blocked issue whose only PR was closed unmerged

Pinned (blocked label, one CLOSED unmerged PR) -> blocked in test_triage_stage::test_derive_stage. Behaviour was already correct so the case passed on arrival; non-vacuity shown by a temporary mutant in derive_stage (any PR unblocks) that made exactly this case fail, then reverted. Commit f44467a9.

<!-- fr:journal kind=finding scope=plan id=r-p2-repos-doc-resolved created=2026-09-21T19:57:19 state=fixed resolves=r-p2-repos-doc -->
### r-p2-repos-doc-resolved · finding [fixed] · resolves r-p2-repos-doc: Facts.repos includes skipped repos, and phase 3 might present its length as repos collected

Facts docstring states repos is the scope including skipped repos; new Facts.collected property (repos minus skipped, scope order). The docstring needs no red test; the property got one, RED then GREEN: test_triage_model::test_collected_is_the_scope_minus_the_skipped_repos. Commit f44467a9.

<!-- fr:journal kind=finding scope=plan id=r-p2-type-nit-resolved created=2026-09-21T19:57:20 state=fixed resolves=r-p2-type-nit -->
### r-p2-type-nit-resolved · finding [fixed] · resolves r-p2-type-nit: _issue(state: str) needs a type: ignore; type it as IssueState

_issue(state: IssueState) and a typed local in collect_facts; the type: ignore is gone and mypy is clean. Typing only, so no red test by design. Commit f44467a9.

<!-- fr:journal kind=finding scope=plan id=r-p2-seam-ast-resolved created=2026-09-21T19:57:20 state=fixed resolves=r-p2-seam-ast -->
### r-p2-seam-ast-resolved · finding [fixed] · resolves r-p2-seam-ast: The seam test is a source substring match: aliased imports and direct subprocess calls slip past it

test_fr_triage_touches_gh_only_in_collect now AST-walks rglob('*.py') over fr/triage plus triage_cmd, forbidding outside collect.py: import fr.gh, from fr import gh (alias/grouping/relative), from fr.gh import ..., any subprocess import, dynamic import_module/__import__. Non-vacuity: a real plant (fr/triage/_seam_plant.py with from fr import (gh) + import subprocess; fr/triage/_plantdir/deep.py with from fr.gh import list_repos) passed the OLD test and failed the NEW one on both files, then removed; permanently, test_the_seam_check_catches_every_spelling_of_a_forge_import (14 plants) and test_the_seam_check_does_not_flag_lookalikes. Commit 9e6ddf62.

<!-- fr:journal kind=review scope=plan id=review-p2 created=2026-09-21T19:58:17 phase=2 -->
### review-p2 · review · Phase 2 review: ready with fixes; all nine fixed and independently re-verified (phase 2)

superpowers:requesting-code-review over spec 3.B-E, 02.yaml and 9be3e832..d287c804, then superpowers:receiving-code-review, with each finding verified in the code before acting (both Importants confirmed at collect.py:178-180 and :237-238). Held: reference-keyed inversion with the synthetic cross-repo record reusing the captured shape; the 3.E stage table row by row, including draft+merged -> merged in any order; one home for Scope and state_dir; a clean seam (GhError and FileNotFoundError both mapped to ForgeError); fr.gh changes backward compatible, with every caller checked; strict models with the key grammar enforced. Raised: 2 Important (r-p2-unviewed, r-p2-case) and 7 Minor, all filed against phase 2; plus r-p2-check-sets and r-p2-render-model filed against PHASE 3, where they gate that review. The spec was amended first for 3.C, 3.D, 3.E and 3.F, adding a fourth check set, unreachable. Fixes were delegated to an executor (7 commits, 829c1acf..a21a6c58), and the orchestrator re-verified them independently: 206 targeted tests pass, fr journal check reports only the two phase-3 findings open, no planted seam-test files remain, and the worktree is clean. The seam test's non-vacuity is proven: the OLD substring test passed with planted aliased and nested imports in place; the new AST test flags both. None refuted.

<!-- fr:journal kind=discovery scope=plan id=p3-real-board created=2026-09-21T20:07:17 phase=3 -->
### p3-real-board · discovery · The real super-fr board renders: 37 issues, 65,368 bytes, PR window full as predicted (phase 3)

Ran from the worktree on 2026-09-21: fr triage collect --repo derio-net/super-fr, then fr triage render --repo derio-net/super-fr (default state dir $HOME/.cache/fr/triage/derio-net--super-fr/). collect wrote facts.json with 37 open issues and printed exactly one warning: the prs list returned exactly its limit (200), possibly truncated, raise with --pr-limit (spec 4 predicted this). render exited 0 and wrote triage.html, 65,368 bytes, 37 rows; it parses cleanly with html.parser. check --json on the same state: 37 unranked, 0 settled, 0 orphaned, 0 unreachable, as expected with no judgements.yaml yet. Not opened in a browser (not needed: the file existing and parsing is the evidence).

<!-- fr:journal kind=discovery scope=plan id=p3-decisions created=2026-09-21T20:07:18 phase=3 -->
### p3-decisions · discovery · Phase 3: decisions beyond the plan's letter (phase 3)

1. check reports FOUR sets (unranked, settled, orphaned, unreachable) per the amended spec 3.F; P3.T1's text names three and is superseded. Unreachable matches a key against Facts.unviewed via normalize_key and against Facts.skipped via issue_key(skipped.repo, n), so no second normaliser exists. 2. render calls check.classify for its Unranked tier and masthead counts, so the board and check can never disagree about what is unranked; render also lists truncation warnings, skipped repos, unreachable and orphaned judgements as masthead notes. 3. The unranked section is always rendered (with an empty-state line when every open issue is judged), as is every declared tier. 4. T2.S3 and T3.S3 refactors (patterns compiled at module level; CSS, SCRIPT and FILTER_BAR as module constants) landed within their GREEN steps, so the REFACTOR steps were re-runs only. 5. check's item lines and the missing-facts error print with soft_wrap=True: rich otherwise hard-wraps at terminal width, which split the collect command across lines in a test and would split long titles, breaking verbatim output. 6. Issue and PR hrefs are emitted only for https:// URLs. 7. Filter chips AND together; in-flight = pr-draft/pr-ready, untouched = backlog/blocked, done = closed/merged. localStorage stores only sort and chips, not the search text.

<!-- fr:journal kind=finding scope=plan id=r-p2-check-sets-resolved created=2026-09-21T20:07:26 state=fixed resolves=r-p2-check-sets -->
### r-p2-check-sets-resolved · finding [fixed] · resolves r-p2-check-sets: check must report unreachable (unviewed or skipped-repo) judgements separately from orphaned, via the shared key normaliser

check.classify reports unranked/settled/orphaned/unreachable; a judgement in Facts.unviewed or a skipped repo is unreachable with its reason, never orphaned; keys compare only via normalize_key and issue_key. Tests: test_triage_check.py::test_a_judgement_the_forge_would_not_show_is_unreachable_not_orphaned, ::test_a_judgement_in_a_skipped_repo_is_unreachable_not_orphaned, ::test_keys_compare_through_the_one_normaliser, ::test_check_json_emits_the_four_sets (478a57ea).

<!-- fr:journal kind=finding scope=plan id=r-p2-render-model-resolved created=2026-09-21T20:07:26 state=fixed resolves=r-p2-render-model -->
### r-p2-render-model-resolved · finding [fixed] · resolves r-p2-render-model: render must build from the Facts model, never raw JSON, because stage is derived and not on disk

render(facts: Facts, judgements: Judgements) takes the model; the command loads via fr.triage.model.load_facts/load_judgements and every row's data-stage and stage pill come from Issue.stage. Test: test_triage_render.py::test_stage_comes_from_the_model_not_from_facts_json asserts facts.json on disk has no stage key while every row carries data-stage equal to issue.stage (pr-draft for #529, blocked for #333) (06e333b2).

<!-- fr:journal kind=finding scope=plan id=r-p3-row-overclaims created=2026-09-21T20:16:14 phase=3 state=open -->
### r-p3-row-overclaims · finding [open] · triage-untrusted-text-inert is at ci, but no test covers the attribute or href contexts (phase 3)

Review Important 2, verified. The hostile fixture only places payloads in title, body and labels, which land in element content. Untested: attribute breakout (a title with a double quote and onmouseover= reaching data-search), non-https URLs (javascript:alert(1), HTTPS://x, and leading whitespace must produce no href), and hostile text in theme, tier title/description, pattern title/ids and Unviewed/Skipped reasons in the masthead. The code IS correct (no XSS path found), but a refactor dropping esc() from attr_s, or loosening _safe_url, would stay green while the matrix claims safety: a false green. Add the tests, then re-assert the row with fr acceptance set-status naming them.

<!-- fr:journal kind=finding scope=plan id=r-p3-syspath created=2026-09-21T20:16:14 phase=3 state=open -->
### r-p3-syspath · finding [open] · test_triage_render.py imports test_triage_collect through a sys.path insert, against the repo convention (phase 3)

Review Important 1, verified at test_triage_render.py:44-45. tests/unit is a package and the convention is package imports (from tests.unit.fakes import ...). The insert loads the module a second time as a top-level name, leaves tests/unit on sys.path for the session (a shadowing hazard), and forces noqa: E402 mid-file imports. Move NOW, SUPER_FR and _super_fr_forge into tests/unit/triage_fixtures.py and import it at the top of both files.

<!-- fr:journal kind=finding scope=plan id=r-p3-inline-nesting created=2026-09-21T20:16:15 phase=3 state=open -->
### r-p3-inline-nesting · finding [open] · Inline markup runs code and bold as two passes, so they overlap and nest invalidly (phase 3)

Review Minor 3, reproduced by the orchestrator: '`a**`b**' -> <code>a<strong></code>b</strong>; '**a`b**c`' -> <strong>a<code>b</strong>c</code>; '`**x**`' -> bold inside code, which should be literal. Not exploitable (element content, escaped text), but invalid markup that breaks code-is-literal. Fix: one pass with an alternation so matches cannot overlap and code content is never rescanned. Add all three as parametrised cases.

<!-- fr:journal kind=finding scope=plan id=r-p3-clock-regex created=2026-09-21T20:16:15 phase=3 state=open -->
### r-p3-clock-regex · finding [open] · The render-reads-no-clock check is a narrow line regex (phase 3)

Review Minor 4: it misses import os, time and other spellings. Make it an AST check that reuses the seam test's walker (forbid time and datetime imports in render.py). The behavioural test stays.

<!-- fr:journal kind=finding scope=plan id=r-p3-classify-twice created=2026-09-21T20:16:16 phase=3 state=open -->
### r-p3-classify-twice · finding [open] · classify() runs twice per render (phase 3)

Review Minor 5: once in _masthead and once in render. Compute it once in render and pass it down, which makes one source of truth literal.

<!-- fr:journal kind=finding scope=plan id=r-p3-softwrap created=2026-09-21T20:16:16 phase=3 state=open -->
### r-p3-softwrap · finding [open] · soft_wrap is not applied everywhere forge text prints (phase 3)

Review Minor 6: _load_state prints TriageError messages (which can quote judgement keys) without soft_wrap, and phase 2's _report prints skipped/unviewed reasons and repo names without it. Apply it consistently wherever forge or judgement text is printed.

<!-- fr:journal kind=finding scope=plan id=r-p3-num-overflow created=2026-09-21T20:16:16 phase=3 state=open -->
### r-p3-num-overflow · finding [open] · The .num label has flex: none and no overflow-wrap, so a long org repo name could overflow at 400px (phase 3)

Review Minor 7. In org scope the label is repo#n; a repo name over ~40 characters overflows. Add overflow-wrap: anywhere and min-width: 0. The orchestrator's live check at 375px showed zero overflow on super-fr, whose names are short, so this is about org scope.

<!-- fr:journal kind=finding scope=plan id=r-p3-copy created=2026-09-21T20:16:17 phase=3 state=open -->
### r-p3-copy · finding [open] · Masthead copy: '1 repos', 'The prs list', and the truncation warning does not name --pr-limit (phase 3)

Found by the orchestrator's visual check of the real super-fr board at 1280px. The count pill pluralises unconditionally ('1 repos'); the warning names its source as 'prs' rather than 'PR'; and spec 3.C says the warning points users at --pr-limit, but the rendered text does not. Pluralise correctly, name sources in plain words, and name the flag in the warning (--pr-limit for PRs; the issue and repo lists have no flag, so say so plainly rather than invent one).

<!-- fr:journal kind=finding scope=plan id=r-p3-spec-timestamps created=2026-09-21T20:16:17 phase=3 state=fixed -->
### r-p3-spec-timestamps · finding [fixed] · Spec 3.G said collected_at is the only timestamp shown; render also shows ranked_at and filed dates (phase 3)

Review Minor 8. All are data, not clock reads, so determinism holds. FIXED in the spec: 3.G now says every timestamp shown is read from facts or judgements, never the clock.

<!-- fr:journal kind=finding scope=plan id=r-p3-plan-three-sets created=2026-09-21T20:16:18 phase=3 state=fixed -->
### r-p3-plan-three-sets · finding [fixed] · Plan P3.T1 still said three sets after the spec moved to four (phase 3)

Review Minor 9. FIXED by editing P3.T1.S1 and P3.T1.S3 in 03.yaml surgically (a hand edit of two text scalars, not an fr plan edit round-trip, which would re-serialise the whole file per #502), stating that the step originally said three sets. fr plan self-review passes afterwards.

<!-- fr:journal kind=finding scope=plan id=r-p3-row-overclaims-resolved created=2026-09-21T20:21:58 state=fixed resolves=r-p3-row-overclaims -->
### r-p3-row-overclaims-resolved · finding [fixed] · resolves r-p3-row-overclaims: triage-untrusted-text-inert is at ci, but no test covers the attribute or href contexts

fbaf57c5 + fbc95430. tests/unit/test_triage_render.py adds test_a_title_cannot_break_out_of_the_data_search_attribute (parsed page: no on* attribute anywhere, literal quote inside data-search), test_a_non_https_url_produces_no_href (javascript:, data:, HTTPS://, leading whitespace), test_hostile_text_in_every_context_renders_escaped (theme, tier title, tier description, pattern title, skipped repo+reason, unviewed reason, scope) and test_hostile_pattern_ids_render_escaped_even_past_the_loader. All passed on first run (code was correct), so each was proven by mutation: removing each of the 9 esc()/inline() calls, or loosening _safe_url (strip+lower; accept-all), turned exactly its test red, 11/11. Mutation runs used PYTHONDONTWRITEBYTECODE=1: a same-size edit within one second otherwise reuses the stale .pyc and reports the previous mutation's result. Row re-asserted via fr acceptance set-status naming the tests and the proof.

<!-- fr:journal kind=finding scope=plan id=r-p3-syspath-resolved created=2026-09-21T20:22:01 state=fixed resolves=r-p3-syspath -->
### r-p3-syspath-resolved · finding [fixed] · resolves r-p3-syspath: test_triage_render.py imports test_triage_collect through a sys.path insert, against the repo convention

4f712a85. tests/unit/triage_fixtures.py now holds NOW, SUPER_FR, ISSUES, PRS, FakeForge and _super_fr_forge (plus the AST walker, as forbidden_imports); test_triage_collect.py and test_triage_render.py import them with from tests.unit.triage_fixtures import ... at the top. No sys.path insert, no noqa: E402 remain. Pure refactor: the 134 triage tests were green before and after.

<!-- fr:journal kind=finding scope=plan id=r-p3-inline-nesting-resolved created=2026-09-21T20:22:33 state=fixed resolves=r-p3-inline-nesting -->
### r-p3-inline-nesting-resolved · finding [fixed] · resolves r-p3-inline-nesting: Inline markup runs code and bold as two passes, so they overlap and nest invalidly

One-pass alternation in render.inline (_INLINE); a bold span's inner text alone is scanned for code, so code-in-bold nests validly and code is never rescanned. test_inline_escapes_first_then_allows_only_code_and_bold gains the three reproductions (`a**`b** -> <code>a**</code>b**; **a`b**c` -> <strong>a`b</strong>c`; `**x**` -> <code>**x**</code>) plus **`x`** -> <strong><code>x</code></strong>; all four were seen RED before the fix, the nine existing allowlist cases stay green.

<!-- fr:journal kind=finding scope=plan id=r-p3-clock-regex-resolved created=2026-09-21T20:23:10 state=fixed resolves=r-p3-clock-regex -->
### r-p3-clock-regex-resolved · finding [fixed] · resolves r-p3-clock-regex: The render-reads-no-clock check is a narrow line regex

test_render_reads_no_clock now calls tests.unit.triage_fixtures.forbidden_imports(path, 'fr.triage', ('time','datetime')), the seam test's walker generalised with a modules argument rather than copied. test_the_clock_check_catches_every_spelling (8 plants) was RED against the old regex for 'import os, time', importlib.import_module('datetime') and __import__('time'), GREEN on the walker; test_the_clock_check_does_not_flag_lookalikes pins timeit and a 'time' string. Planting 'import os, time' in render.py turns test_render_reads_no_clock red. The behavioural byte-identical test is unchanged.

<!-- fr:journal kind=finding scope=plan id=r-p3-classify-twice-resolved created=2026-09-21T20:23:31 state=fixed resolves=r-p3-classify-twice -->
### r-p3-classify-twice-resolved · finding [fixed] · resolves r-p3-classify-twice: classify() runs twice per render

render() calls classify once and passes the CheckResult to _masthead, which no longer classifies; the Unranked tier reads the same result. test_render_classifies_once counts calls through a monkeypatched classify: RED at 2, GREEN at 1.

<!-- fr:journal kind=finding scope=plan id=r-p3-softwrap-resolved created=2026-09-21T20:25:07 state=fixed resolves=r-p3-softwrap -->
### r-p3-softwrap-resolved · finding [fixed] · resolves r-p3-softwrap: soft_wrap is not applied everywhere forge text prints

triage_cmd: soft_wrap=True on _report's skipped, unviewed and truncation prints, both TriageError prints (collect, _load_state), the input-echoing usage errors and both 'wrote' lines. tests/unit/test_triage_cli.py: test_a_long_skipped_reason_prints_on_one_line, test_a_long_unviewed_reason_prints_on_one_line, test_a_long_forge_error_prints_on_one_line, test_an_unreadable_state_file_error_prints_on_one_line[check|render], all RED then GREEN. They set COLUMNS=40 via a narrow fixture: tests/conftest.py pins COLUMNS=200 for the suite, and the first draft without it passed vacuously.

<!-- fr:journal kind=finding scope=plan id=r-p3-num-overflow-resolved created=2026-09-21T20:26:03 state=fixed resolves=r-p3-num-overflow -->
### r-p3-num-overflow-resolved · finding [fixed] · resolves r-p3-num-overflow: The .num label has flex: none and no overflow-wrap, so a long org repo name could overflow at 400px

.num: overflow-wrap: anywhere; min-width: 0; and flex: none -> flex: 0 1 auto. Deviation from the finding's two-declaration fix, measured: in headless Chrome (window 400 requested, 485 actual minimum) an org-scope board with a 64-char repo name overflowed on 12/12 .num labels with the old rule, still 12/12 with overflow-wrap + min-width added to flex: none (a non-shrinking item stays at max-content width), and 0/12 once the item can shrink. test_a_long_repo_label_can_wrap_instead_of_overflowing pins all three declarations (RED, then GREEN).

<!-- fr:journal kind=finding scope=plan id=r-p3-copy-resolved created=2026-09-21T20:27:06 state=fixed resolves=r-p3-copy -->
### r-p3-copy-resolved · finding [fixed] · resolves r-p3-copy: Masthead copy: '1 repos', 'The prs list', and the truncation warning does not name --pr-limit

render.noun/plural pluralise the repo count pill (1 repo, 2 repos) and both 'wrote' lines. Truncation.describe(target) is the single wording shared by the board and collect's CLI warning: PR list / issue list / repo list; the PR sentence ends 'raise it with --pr-limit', the issue and repo ones 'no flag raises this limit'. Tests, all RED first: test_counts_pluralise[0,1,2,37], test_one_collected_repo_is_one_repo_not_one_repos, test_two_collected_repos_are_repos, test_a_truncation_note_names_its_list_in_plain_words_and_its_remedy[prs|issues|repos] (asserts no '--' for the flagless lists), and test_triage_cli.py::test_pr_limit_widens_the_window_and_a_full_list_warns now asserts the full sentence.

<!-- fr:journal kind=discovery scope=plan id=p3-real-board-rerender created=2026-09-21T20:27:38 phase=3 -->
### p3-real-board-rerender · discovery · The real super-fr board after the phase-3 review fixes: 37 rows, 65,437 bytes, one PR-list warning (supersedes p3-real-board's byte count) (phase 3)

Re-run from the worktree on 2026-09-21 after the eight r-p3-* fixes: fr triage collect --repo derio-net/super-fr (exit 0), then fr triage render --repo derio-net/super-fr (exit 0), default state dir $HOME/.cache/fr/triage/derio-net--super-fr/. collect printed exactly one warning, now in the new wording: the PR list for derio-net/super-fr returned exactly its limit (200), so it may be cut short; raise it with --pr-limit. It reported 37 open issues. triage.html is 65,437 bytes with 37 rows and parses cleanly with html.parser; the masthead counts read 37 open, 37 unranked, 1 in flight, 0 settled, 1 repo (singular, r-p3-copy), and its only note is the same PR-list warning. This SUPERSEDES the byte count in p3-real-board (65,368): that figure had already stopped matching the file on disk (65,150) before this run, and the journal is append-only, so it is corrected here rather than edited. The size moves with live forge data (issue bodies, PR links) as well as with render changes, so treat any byte count as a snapshot.

<!-- fr:journal kind=review scope=plan id=review-p3 created=2026-09-21T20:34:05 phase=3 -->
### review-p3 · review · Phase 3 review: no XSS path; ready with fixes; all eight fixed and re-verified (phase 3)

superpowers:requesting-code-review over spec 3.D-G, 03.yaml and eb8b6092..9d825b4b, aimed hardest at untrusted text, since org scope renders anyone's issue text. Then superpowers:receiving-code-review; the orchestrator reproduced the inline mis-nesting and the sys.path hack before acting. HELD: no XSS path. Attribute breakout, javascript:/data:/HTTPS:// hrefs and crafted backtick and ** input all failed to land; forge text never reaches the script; links fail closed. Also held: determinism, the four sets per amended 3.F, one key normaliser, and render reading Issue.stage from the model. The orchestrator additionally checked the real super-fr board live in a browser: Unranked first, the board's palette, zero horizontal overflow at 375px, and dark-mode tokens exact (#14181A / #E7EBE8). RAISED: 2 Important (r-p3-row-overclaims, r-p3-syspath), 5 Minor, plus 3 copy nits from the visual check (r-p3-copy), all against phase 3; r-p3-spec-timestamps and r-p3-plan-three-sets were fixed by the orchestrator. The most important was r-p3-row-overclaims: the triage-untrusted-text-inert row was at ci while its tests covered only element content, a latent false green. FIXED with attribute, href and masthead-context tests, each proven by an 11-case mutation run (the exact named test went red, then was reverted), re-run with PYTHONDONTWRITEBYTECODE=1 after a stale .pyc gave a false result. Notable executor catches: the reviewer's CSS fix was incomplete (flex: none never shrinks; measured 12/12 overflow until flex: 0 1 auto), and conftest's COLUMNS=200 masked the soft_wrap tests until they set their own width. Orchestrator re-verification: tree clean, 229 targeted tests pass, journal check exit 0, no sys.path insert remains. None refuted.

<!-- fr:journal kind=discovery scope=plan id=p4-deviations created=2026-09-21T20:38:11 phase=4 -->
### p4-deviations · discovery · Phase 4: three deviations from the plan's letter (phase 4)

1. OMITTED the fr migrate artifacts --yes line P4.T1.S2 asked for: triage is in READ_ONLY_COMMANDS since phase 1, so the migration gate can never refuse it and the line would describe an impossible state. 2. The skill carries a compact judgements.yaml example instead of pointing at fr triage --help, because --help does not document the file; the example is validated against fr.triage.model.Judgements (loads clean). 3. The skill describes check's FOUR sets (the amended spec 3.F), including never pruning unreachable. Also: the mirror generators write a .source provenance note per skill, tracked for every other skill; committed separately (e4cc2c93) after the skill commit missed them. The writing-skills GREEN re-run (skill-green) still needs a second agent and is owed to the phase review, per eec2c9fa6567.

<!-- fr:journal kind=discovery scope=plan id=skill-green created=2026-09-21T20:48:50 phase=4 -->
### skill-green · discovery · GREEN run: the skill routed an agent through the engine at 37-issue scale, but its delta focus hid a duplicate the baseline caught (phase 4)

writing-skills GREEN run, the counterpart to skill-baseline. Scenario: the LIVE derio-net/super-fr backlog (37 open), seeded as yesterday's triage with 30 judgements and 7 left unranked. Among the 7: #463 is already fixed on main (a stale claim) and #526 duplicates #439, which was among the SEEDED judgements. A fake super-fr#99999 judgement tests never prune unreachable. The agent had the skill read from its file, forge writes forbidden for safety, and render without --open so no browser opens on the operator's desktop. PASSED: ran collect -> re-collected with --pr-limit 1000 when the PR list hit 200 (per skill step 1) -> check -> judged EXACTLY the 7 unranked, leaving all seeded judgements and #99999 untouched (sync costs the delta) -> render. Verified all 7 against CURRENT main, and went beyond the skill: it noticed the checkout had diverged from main and read origin/main with git show. Caught the stale trap: #463 already fixed by #508, written as an unrun gh close command. Recorded line drift (#438 :1013 -> :1368). Batched by subsystem and added a pattern across four issues. Kept #99999 as unreachable, and flagged on its own that GitHub's reply was a definite 'Could not resolve to an issue', not a failure. Updated ranked_at unprompted. Reported path + delta + commands, not the whole ranking. No forge writes. FAILED: did NOT link #526 as a duplicate of the already-judged #439. The skill caused this: its delta focus ('leave existing judgements alone', 'a refresh costs the delta') narrowed attention to the unranked batch, and a new issue's likeliest duplicate is an OLD one. The baseline, seeing all six together, caught it. REFACTOR owed: duplicate-hunting for each unranked issue spans the WHOLE judged set, and ranked_at should be stated explicitly. SECONDARY: GitHub does distinguish a definite not-found ('Could not resolve to an issue') from a transient failure, so the spec's claim (r-p2-unviewed) that only the forge could tell them apart and does not is too strong. See the phase-4 review for the resolution.

<!-- fr:journal kind=finding scope=plan id=r-p4-orphaned-meaning created=2026-09-21T20:54:03 phase=4 state=fixed -->
### r-p4-orphaned-meaning · finding [fixed] · The skill, spec, check.py and AGENTS.md said a deleted issue is orphaned; it is unreachable, so under 'never prune unreachable' it could never be cleaned up (phase 4)

Phase-4 review C1, suspected by the orchestrator on reading the skill against the engine and reproduced by the reviewer. collect views every judged key in a collected repo, and every view failure goes to unviewed -> unreachable; only a key naming no collected repo reaches orphaned. So a deleted issue and a typo'd number are unreachable. The root was the orchestrator's own r-p2-unviewed spec amendment, which moved view failures out of orphaned but left orphaned's definition saying 'deleted issue'. FIXED: orphaned now means exactly 'names no repo collect read' (SKILL.md step 2, spec 3.F rewritten as four real bullets, check.py docstring, AGENTS.md, the board's own note). The skill tells the agent to confirm a not-found reason with gh issue view and then recommend removal, so a deleted issue has a path out.

<!-- fr:journal kind=finding scope=plan id=r-p4-stale-facts-orphan created=2026-09-21T20:54:03 phase=4 state=fixed -->
### r-p4-stale-facts-orphan · finding [fixed] · check called a judgement added after the last collect orphaned, even for an issue that exists (phase 4)

Phase-4 review C2, reproduced by the orchestrator while seeding the GREEN run (super-fr#99999 read as orphaned because the judgements postdated the collect) and by the reviewer. check reads the LAST collect's facts; a key added since was never viewed. FIXED in the ENGINE, structurally, as the reviewer preferred: _unreachable_reason returns JUDGED_AFTER_COLLECT for a key in a collected repo that is in neither issues nor unviewed, so orphaned is true by construction. New test test_a_key_in_a_collected_repo_absent_from_the_facts_is_unreachable_not_orphaned, RED then GREEN. Three existing tests had asserted the defect: two used super-fr#404 as 'a deleted issue', a state no real collect can produce, and one used a collected repo's key as its orphan. They were corrected, with a comment in each saying why.

<!-- fr:journal kind=finding scope=plan id=r-p4-limit-loop created=2026-09-21T20:54:04 phase=4 state=fixed -->
### r-p4-limit-loop · finding [fixed] · The skill told the agent to re-run with --pr-limit whenever ANY list hit its limit, which loops on super-fr itself (phase 4)

Phase-4 review I1. Only the PR list has a flag; issue and repo warnings say 'no flag raises this limit'. super-fr's PR list always fills the 200 window, so 'before judging anything' had no exit. FIXED: re-run once with --pr-limit 1000 for the PR list only; if it still warns, go on and say so; issue and repo warnings mean go on and say rows may be missing.

<!-- fr:journal kind=finding scope=plan id=r-p4-green-dup-miss created=2026-09-21T20:54:04 phase=4 state=fixed -->
### r-p4-green-dup-miss · finding [fixed] · GREEN run: the skill's own delta focus hid a duplicate the baseline caught (phase 4)

From skill-green. The agent judged only the unranked batch and never compared #526 against the already-judged #439; the baseline, seeing all six together, caught it. The skill caused this ('leave existing judgements alone', 'a refresh costs the delta'). FIXED in step 3: leave existing judgements alone, but compare every new issue against ALL of them, because its likeliest duplicate is an old issue. Re-tested in GREEN-2.

<!-- fr:journal kind=finding scope=plan id=r-p4-minors created=2026-09-21T20:54:05 phase=4 state=fixed -->
### r-p4-minors · finding [fixed] · Five documentation gaps: ranked_at upkeep, first run, scope flags on every command, truncated bodies, and a 'facts changed' check the engine never surfaces (phase 4)

Phase-4 review M1-M5, all fixed in SKILL.md: set ranked_at to today on every change; on a first run create judgements.yaml with schema and tiers first; pass the same --repo/--org/--dir to every command; bodies stop at 2,000 characters, so use gh issue view when cut off; and the undetectable 'unless their facts changed' clause is removed. M6 (spec 3.F announcing four sets while listing three) is fixed with r-p4-orphaned-meaning. The skill is now 111 lines, under the 120 cap, and both mirrors were regenerated and checked.

<!-- fr:journal kind=discovery scope=plan id=skill-green-2 created=2026-09-21T20:56:50 phase=4 -->
### skill-green-2 · discovery · GREEN-2: the refactored skill closes the duplicate miss, and the not-found path works (phase 4)

writing-skills re-test after REFACTOR (1b7f766c). Same scenario and prompt as skill-green: live super-fr, 30 seeded judgements, 7 unranked, the #463 stale trap, the #526/#439 duplicate, and the super-fr#99999 bad key. Re-seeded fresh. PASSED everything GREEN-1 passed: re-collected with --pr-limit 1000; judged exactly the 7; verified at current origin/main, noticing the branch lags main; #463 became a close command; line drift recorded; batches and a pattern; path + delta report; no forge writes. NOW ALSO PASSES the item GREEN-1 failed: it linked #526 and #439 as duplicates, and reasoned about which to keep by re-checking #439's own 'fails safe' claim against the code. The not-found path works too: it confirmed super-fr#99999 does not exist with gh issue view, recommended removal, and did not prune it itself. Minor: it wrote a helper script in the scratchpad outside the triage dir, which is harmless and not in the repo. The RED -> GREEN -> REFACTOR -> re-test cycle is closed.

<!-- fr:journal kind=discovery scope=plan id=archive-gate-unmerged created=2026-09-21T20:56:50 -->
### archive-gate-unmerged · discovery · Out of scope, real: fr archive would archive an UNMERGED fr-goal plan, so #439's 'fails safe' argument does not hold

Found by the GREEN-2 agent while choosing which of #439/#526 to keep; verified by the orchestrator at fr/render.py:256. archive_gate clears any phase where tracking_issue is None and plan_locally_complete(phase), with no merge check. fr-goal plans are never dispatched (implement runs in the workspace, and fr apply refuses before merge), so their phases never get a tracking_issue. For the most common plan kind in this repo, then, fr status's 'merged but not archived' and fr archive --all together would archive work that never merged. #439 argued it was 'only a wording bug' because 'archive_gate performs the real check'; for fr-goal plans it does not. Mitigated today only by fr-goal's post-merge prose ('first verify it reached main via fr isolation verify-merge'). Not fixed here; out of scope for fr triage. Recommend folding it into #439/#526 when fixed, since it changes their severity from wording to a silent-wrongness path.

<!-- fr:journal kind=review scope=plan id=review-p4 created=2026-09-21T21:00:27 phase=4 -->
### review-p4 · review · Phase 4 review: two engine-truth defects in the skill fixed structurally; the writing-skills cycle closed (phase 4)

Two independent reviews in parallel. (1) superpowers:requesting-code-review over the skill, mirrors, fr skills entry, AGENTS.md and the 4.14.0 bump (f6620e4f..32540034), aimed at two inaccuracies the orchestrator found by reading the skill against the engine; then superpowers:receiving-code-review, with both reproduced before acting. (2) The writing-skills GREEN run on the live 37-issue backlog, seeded as a prior triage. Held: release mechanics (all 10 version surfaces at 4.14.0, one commit), both mirrors in sync with .source files committed, harness neutrality, a description that says only when to use, and the skill targeting skill-baseline's structural failures. Raised and FIXED: r-p4-orphaned-meaning (C1: a deleted issue is unreachable, not orphaned; spec 3.F rewritten as four real bullets); r-p4-stale-facts-orphan (C2: fixed in the ENGINE, so a key judged after the last collect is unreachable and orphaned is true by construction; three tests that had asserted the defect corrected); r-p4-limit-loop (I1); r-p4-green-dup-miss (GREEN's one failure, caused by the skill's own delta focus); r-p4-minors (M1-M6). I2 (GREEN owed) was already running in parallel and was then re-tested after the refactor as skill-green-2, which passes the duplicate item GREEN failed and exercises the new not-found path. Out of scope, journalled: archive-gate-unmerged, found by GREEN-2 and verified at fr/render.py:256. Gate: full suite 4235 passed / 88 skipped / 0 failed (exit 0); ruff, format and mypy (154 files) clean; both mirrors --check clean; SKILL.md 111 lines. None refuted.
