# Journal: 2026-09-21-fr-triage

<!-- fr:journal kind=discovery scope=plan id=6dac56d140b6 created=2026-09-21T19:08:17 phase=3 -->
### 6dac56d140b6 · discovery · no-refactor-because P3.T5 (phase 3)

Status flips via fr acceptance set-status plus the quality gate. No production code is written in this task, so there is nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=eec2c9fa6567 created=2026-09-21T19:08:18 phase=4 -->
### eec2c9fa6567 · discovery · no-refactor-because P4.T1 (phase 4)

The skill is prose, and its refactor is writing-skills' REFACTOR cycle: re-run the baseline scenario WITH the skill and close any loophole it finds. That re-run needs a second agent, which the phase executor cannot spawn, so it happens in this phase's review, not in a task step. Recorded there as skill-green.

<!-- fr:journal kind=discovery scope=plan id=b37977e7cb24 created=2026-09-21T19:08:18 phase=4 -->
### b37977e7cb24 · discovery · no-refactor-because P4.T2 (phase 4)

Runs the two mirror generators and their guards. Generated files are never hand-edited (AGENTS.md), so there is nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=bf9104fdf073 created=2026-09-21T19:08:18 phase=4 -->
### bf9104fdf073 · discovery · no-refactor-because P4.T3 (phase 4)

One table entry in skills_cmd.SKILLS and one AGENTS.md bullet. Nothing structural to clean up.

<!-- fr:journal kind=discovery scope=plan id=cc920bebc761 created=2026-09-21T19:08:19 phase=4 -->
### cc920bebc761 · discovery · no-refactor-because P4.T4 (phase 4)

Version bump through scripts/bump-version.py plus the full gate. No code of its own.

<!-- fr:journal kind=discovery scope=plan id=46169531b87e created=2026-09-21T19:08:28 phase=1 -->
### 46169531b87e · discovery · no-refactor-because P1.T3 (phase 1)

Adds one name to the READ_ONLY_COMMANDS tuple with its argument, updates the pinned test literal, and runs the skeleton's quality gate. A one-token change leaves nothing to refactor.

<!-- fr:journal kind=discovery scope=plan id=9236e15d4000 created=2026-09-21T19:08:28 phase=2 -->
### 9236e15d4000 · discovery · no-refactor-because P2.T5 (phase 2)

Status flips via fr acceptance set-status plus the quality gate. No production code is written in this task.

<!-- fr:journal kind=discovery scope=plan id=227fbb293b8f created=2026-09-21T19:08:40 phase=1 -->
### 227fbb293b8f · discovery · no-refactor-because P1.T1 (phase 1)

Captures two gh outputs verbatim and writes a provenance README. Captured fixtures must not be reshaped, and refactoring them would be exactly the construction the capture rule forbids.

<!-- fr:journal kind=discovery scope=plan id=skill-baseline created=2026-09-21T19:12:59 phase=4 -->
### skill-baseline · discovery · RED baseline: an agent triaging WITHOUT fr-triage caught both planted traps — the skill must target what it actually lacked (phase 4)

writing-skills RED run, recorded verbatim in substance and not softened. Scenario: six real super-fr issues, bodies captured with gh (not constructed), two planted traps drawn from real mistakes: #493 quoting a parity.yaml row as absent when main says enforced (the exact claim the hand-built board ranked as live), and #439/#526 as a duplicate pair. Pressure applied: a ten-minute deadline and a waiting operator. Forge writes were forbidden for safety, since the repo is real and public. RESULT: the baseline did NOT fail the traps. It re-read parity.yaml and called #493 fixed; it flagged #439/#526 as one bug; it noticed #438's cited line drifted from :1013 to :1312; it found #463 already fixed on main (test sets HOME to an empty dir); it ranked by cost of failure; it batched #438 with #469 by subsystem; and it wrote forge actions as commands rather than running them. It also found a real out-of-scope bug (recorded separately as discovery bare-marketplace-path). Every claim was re-verified by the orchestrator in the worktree at 089fb51d; all three novel ones hold. CONCLUSION for P4: at six issues, verify-before-claim is not where an agent fails, so the skill must not spend its 120 lines lecturing on it. The failures actually observed are structural: (1) the output lived only in chat, so the next triage starts from zero; (2) there is no notion of what changed since the last triage, which is exactly how the hand-built board silently lacked four issues; (3) nothing re-runnable, so sync means redoing it. The engine closes those. The verification failure that DID happen was the orchestrator's own, at 41-issue scale, trusting #493's quoted YAML, and this six-issue baseline does not reproduce that pressure. The skill therefore keeps ONE compact verification rule, framed around scale and line drift, and treats the baseline's good behaviours (note drift, forge actions as unrun commands, batch by subsystem) as the required shape of a judgement, not as lessons.

<!-- fr:journal kind=discovery scope=plan id=bare-marketplace-path created=2026-09-21T19:13:10 -->
### bare-marketplace-path · discovery · Out of scope, real: local.py:1301-1302 still points users at the retired bare derio-net marketplace

Found by the RED baseline agent, confirmed by the orchestrator at 089fb51d. The IsolationError for a missing executable scripts/validate-plans.sh tells the user to run bash ~/.claude/plugins/marketplaces/derio-net/scripts/install-validator-wrapper.sh. That is the bare org name AGENTS.md records as retired and purged by both installers, so the advice points at a directory that no longer exists. The sibling message at :1281 already uses derio-net--super-fr. It survived the rename because the path is split across two implicitly concatenated string literals (marketplaces/ then derio-net/scripts/...), so no whole-path grep can match it. The orchestrator's own first grep missed it the same way. Not fixed in this PR: out of scope for fr triage. Recorded as a discovery rather than an open finding because fr journal has no state for true but out of scope (#491), and an open finding would block this run's delivery over work it never claimed. Recommend a one-line fix and an issue.

<!-- fr:journal kind=discovery scope=plan id=c5e5188ad9e1 created=2026-09-21T19:19:45 phase=1 -->
### c5e5188ad9e1 · discovery · artifact-versioning rule prose still says 'the read-only five' (phase 1)

The artifact-versioning rule's CLI-entry-gate section enumerates the exempt commands as 'migrate plus the read-only five — status, skills, isolation, init, validate'. That was already stale before this phase (harness joined in 2026-09-18) and is now two short with triage. The code and its pinned test (test_the_exemption_list_is_exactly_these_things) are the authority and are correct; only the prose lags. Not edited in phase 1 (out of plan scope); a one-line prose fix is owed before delivery.

<!-- fr:journal kind=finding scope=plan id=r-p1-exempt-prose created=2026-09-21T19:22:33 phase=1 state=fixed -->
### r-p1-exempt-prose · finding [fixed] · artifact-versioning.md named 'the read-only five' for a list that is now seven (phase 1)

Raised by the phase-1 executor as discovery c5e5188ad9e1, in scope because this PR is what makes the list seven. The rule's Exempt commands paragraph counted five and named them; harness had already made it six unnoticed, and triage made seven. FIXED structurally rather than recounted: the prose now names fr.artifacts.trigger.READ_ONLY_COMMANDS as the source of truth and states the membership criterion, and new test test_the_rule_prose_names_every_read_only_command asserts the paragraph names every member of the tuple. Mutation-checked: against the old prose it fails naming exactly ['harness', 'triage'], which also proves the pre-existing harness drift; against the new prose it passes. Both mirror generators re-run; .opencode/instructions/artifact-versioning.md regenerated, and .hermes unchanged because SOUL.d carries only plugin rules.
