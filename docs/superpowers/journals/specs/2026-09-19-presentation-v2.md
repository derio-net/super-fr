# Journal: 2026-09-19-presentation-v2

<!-- fr:journal kind=decision scope=spec id=v2-spine-not-catalogue created=2026-09-19T16:28:36 -->
### v2-spine-not-catalogue · decision · The recorded run is the spine; the four angles annotate it

v1 failed by growing outward from the subject (14 upgrades in build order). Four angles x N upgrades would reproduce that in tidier buckets. Instead one real /fr-goal run carries the narrative and each angle attaches to the moment where it is visible. Consequence: the angles are not peers -- Security bookends, Quality and Continuity interleave, Extensibility surfaces once -- and that ranking matches what an audience whose next action is 'one issue tomorrow' actually needs.

<!-- fr:journal kind=finding scope=spec id=v2-unshipped-extensibility-claims created=2026-09-19T16:28:55 state=open -->
### v2-unshipped-extensibility-claims · finding [open] · Three Extensibility items are not operator-reachable and must not be claimed

Verified against docs/acceptance/matrix.yaml 2026-09-19. multi-repo-spec-fanout: not-implemented, 'fake-runner-only and operator-unobservable'. dispatch-unit-declared-by-shape: not-implemented, only unit:phase is reachable through a shipped path. GitLab/Gitea adapters: unit-verified and reachable from fr apply, but 'not yet proven against a live instance'. The talk promises 'use it immediately', so the first claim that fails when a listener tries it destroys the promise. Coda is restricted to phase executors, model tiers, and the three harnesses. Resolve when either the rows flip or the coda copy is written excluding them.

<!-- fr:journal kind=decision scope=spec id=v2-public-source-private-render created=2026-09-19T16:28:55 -->
### v2-public-source-private-render · decision · Deck source is public and generic; the rendered deck is private

Two separable problems. Repo hygiene: the internal repo is never named here, assets live outside the repo behind a manifest. Content exposure: the real leak is code on screen (file contents, function names, domain) which no gitignore fixes and which sed over a cast cannot reliably redact -- a name can split across write boundaries amid escape sequences. Resolved by scope, not by tooling: the rendered deck is internal-only. The build fails loudly on a missing manifest asset so a checkout without the private assets cannot silently render an incomplete deck. Carries forward v1's constraint that the authoring session is not cleared for the operator's board.

<!-- fr:journal kind=discovery scope=spec id=v2-extensibility-claims-corrected created=2026-09-19T16:44:16 -->
### v2-extensibility-claims-corrected · discovery · Multi-repo specs and custom shapes ARE operator-reachable — my earlier restriction was wrong

Operator corrected me and verification agrees. (1) Multi-repo: fr.spec's rollup explicitly handles 'the normal multi-repo shape' -- a plan folder that does not resolve locally is read via the contents API and given identical phase/step arithmetic, counting toward plans_complete, degrading to Unreachable rather than passing silently. A single spec driving plans across repos is supported and observable. What is NOT implemented is multi-repo-spec-fanout, i.e. automatic per-repo work-item DISPATCH. Two different claims; I conflated them. (2) Custom shapes: fr.workflow.resolve resolves docs/superpowers/workflows/<name>.yaml wholesale over shipped, and v1 authored and ran presentation-showdown through exactly that path. Operator-reachable. What is NOT implemented is dispatch-unit-declared-by-shape, i.e. selecting run- or spec-GRANULARITY for dispatch; only unit:phase is reachable. The coda may claim multi-repo specs and custom shapes; it may not claim spec fan-out dispatch or non-phase dispatch granularity.

<!-- fr:journal kind=finding scope=spec id=v2-glab-contents-broken-live created=2026-09-19T16:44:30 state=open -->
### v2-glab-contents-broken-live · finding [open] · GitLab adapter fails live: contents API omits the mandatory ref parameter

First live exercise of RealGlabClient against a self-hosted GitLab instance (host, group and project redacted per .claude/rules/third-party-privacy.md; default branch master). Results: list_dir works; the issues endpoint is reachable; file_exists and read_file are BROKEN. GitLab's GET /projects/:id/repository/files/:path requires a ref query parameter -- without it the API returns HTTP 400 {'error':'ref is missing, ref is empty'}. real_glabclient.py sends none. Severity is asymmetric: read_file raises loudly, but file_exists swallows every GlabError into False under its documented fail-soft posture, so a malformed request is indistinguishable from an absent file. That is why the unit suite never caught it -- the tests mock _run_glab, so the missing parameter never reaches an API. Verified fix: ?ref=HEAD works and is branch-agnostic (the project's default branch is master, not main). Two further gaps found in the same pass: _run_glab never passes --hostname and GITLAB_HOST appears nowhere in the repo, so there is no documented way to target a self-hosted instance; and _hosts.DEFAULT_HOST_BACKENDS covers only github.com/gitlab.com, so a self-hosted GitLab host resolves to 'github' unless .devcontainer/fr-profiles.yaml declares backend: gitlab. Blocks the coda's GitLab claim until fixed.

<!-- fr:journal kind=decision scope=spec id=v2-no-binaries-in-repo created=2026-09-19T16:44:31 -->
### v2-no-binaries-in-repo · decision · No binaries in super-fr: casts, video and stills live outside the repo entirely

Operator constraint, two independent reasons. Privacy is the lesser one. The load-bearing reason is that super-fr IS a Skill -- it is cloned onto every consumer's machine via the marketplace rsync -- so committed binaries inflate every install forever and can never be removed from history. Decision: casts, rendered video and stills live OUTSIDE the repo (not merely gitignored), reached through an asset manifest of paths plus checksums. NOTE a pre-existing violation this exposes: docs/presentation/version-1/diagrams/ already holds 17 committed PNGs. They predate this rule and are now inherited weight in every clone. Flag for the operator: v2 must not add to it, and removing the existing ones is a separate decision since it needs history rewriting to actually reclaim the bytes.

<!-- fr:journal kind=decision scope=spec id=v2-blocked-on-gitlab-486 created=2026-09-19T18:18:23 -->
### v2-blocked-on-gitlab-486 · decision · Recording is blocked on super-fr#486 — GitLab must work before the run is worth recording

Filed https://github.com/derio-net/super-fr/issues/486 for the ref-parameter defect. The demo does not get recorded until it is fixed AND live-verified. Reason is not demo polish: GitLab is the only forge at the audience's company, so the recording's entire claim is that this works on their stack. A run that fell back to GitHub would prove nothing to them, and the talk's promise is 'use it immediately'. The issue's acceptance deliberately demands live proof rather than unit tests, because unit-level evidence accepted for an integration claim is what produced the bug: multibackend-gitlab-tracking sits at status ci -- green -- for a capability that does not work, since the tests mock the subprocess. The demo run doubles as that live verification.

<!-- fr:journal kind=decision scope=spec id=v2-fork-remote-safety created=2026-09-19T18:18:23 -->
### v2-fork-remote-safety · decision · origin repointed to the operator's fork; the team project is push-disabled

The demo project's origin now points at the operator's own fork (identity redacted per .claude/rules/third-party-privacy.md). The team's project is retained as upstream with its push URL set to DISABLED, verified by dry-run push failing. Rationale: a /fr-goal run opens merge requests unattended, so an unreachable push path on another team's repository is a safety property rather than tidiness; keeping upstream fetch-only preserves the ability to pull course updates. Consequence for the recording: the issue and MR live in the operator's namespace, so labels and issue state can be exercised freely. Identity note -- the bc88 lesson INVERTS here. There the bug was the work identity landing on a public OSS commit; these commits go to the employer's GitLab, so the work identity supplied by the includeIf for the work path is correct, and RUN_ROOT must stay under the work path rather than the personal one.

<!-- fr:journal kind=finding scope=spec id=v2-third-party-leak-scrubbed created=2026-09-19T22:49:10 state=fixed -->
### v2-third-party-leak-scrubbed · finding [fixed] · I leaked third-party identifiers into committed artifacts; scrubbed

The third-party-privacy rule landed on main after I had already committed. Reviewing my own output against it: the employer's GitLab host, group, project, fork namespace and the internal course name reached the spec, this journal, the runbook and two commit messages -- all pushed. Redact-at-capture-time is exactly the discipline I did not apply; I captured live evidence and pasted identity along with shape. What did NOT leak, checked rather than assumed: issue #486 and PR #468 bodies were already written with placeholders. Remediation: working tree scrubbed to shape-only with identities moved to an untracked operator-local env file outside the repo; the two offending commit messages rewritten by squash; force-push with --force-with-lease; local backup ref refs/backup/pre-privacy-scrub kept and never pushed. LIMIT, stated because a green scrub must not imply more than it achieved: a force-push does not unpublish. The pre-rewrite SHAs stay reachable on GitHub until garbage collection, and the PR timeline records them. Separately flagged to the operator: pre-existing leaks of the same host and work email in docs/superpowers/implemented/specs/2026-07-14-isolation-push-diagnostics-design.md and docs/superpowers/journals/plans/2026-09-09-presentation-showdown.md, which are already merged to main and are a larger decision.

<!-- fr:journal kind=finding scope=spec id=v2-glab-contents-broken-live-resolved created=2026-09-19T22:52:30 state=fixed resolves=v2-glab-contents-broken-live -->
### v2-glab-contents-broken-live-resolved · finding [fixed] · resolves v2-glab-contents-broken-live: GitLab adapter fails live: contents API omits the mandatory ref parameter

Fixed by gh-486 / PR #487 and independently re-verified live by me on 2026-09-19 against the same self-hosted instance (host redacted). With backend: gitlab declared and NO GITLAB_HOST exported, detect_backend resolves gitlab, client_for returns RealGlabClient, file_exists is True for two files that exist and False for one that does not, list_dir returns ['main','test'], read_file returns 3009 chars of well-formed XML. The core behavioural fix verified directly too: file_exists against an unreachable host now RAISES rather than returning False, so an error is no longer indistinguishable from absence -- is_not_found narrows the swallow to a genuine 404. The fix went well beyond what I reported: the live walk found three further defects, notably that GitLab returns web_url as /-/work_items/N which fr stored and then could not parse, crashing observe on every re-apply -- affecting gitlab.com identically, so fr apply against ANY GitLab repo worked exactly once. multibackend-gitlab-tracking moved ci -> skipped, the honest level since live verification cannot run in CI. Recording is unblocked.

<!-- fr:journal kind=finding scope=spec id=v2-unshipped-extensibility-claims-resolved created=2026-09-19T22:52:46 state=fixed resolves=v2-unshipped-extensibility-claims -->
### v2-unshipped-extensibility-claims-resolved · finding [fixed] · resolves v2-unshipped-extensibility-claims: Three Extensibility items are not operator-reachable and must not be claimed

Superseded on all three counts, two because I was wrong and one because it was fixed. (1) Multi-repo: I restricted the wrong claim. Multi-repo plans from a single spec ARE supported -- fr.spec's rollup handles them via the contents API. Only per-repo dispatch FAN-OUT is not-implemented. (2) Custom shapes: also wrong. Repo-authored docs/superpowers/workflows/<name>.yaml resolves wholesale over shipped, and v1 ran one. Only non-phase dispatch GRANULARITY is not-implemented. (3) GitLab: fixed by gh-486/PR #487 and re-verified live by me; multibackend-gitlab-tracking moved ci -> skipped with a live-verified note. FINAL COVERAGE for the coda -- claimable: phase executors, model tiers, three harnesses, multi-repo plans from one spec, custom shapes, and GitLab (live-verified, self-hosted). NOT claimable: multi-repo-spec-fanout, dispatch-unit-declared-by-shape, and Gitea, which stays explicitly unproven by agreement.

<!-- fr:journal kind=decision scope=spec id=v2-fr-init-inside-the-recording created=2026-09-19T22:59:37 -->
### v2-fr-init-inside-the-recording · decision · fr-init moves into the recording; the run starts from a pristine repo

Operator decision: the listener will have to run fr-init herself, so the recording shows it. The arc becomes pristine repo -> fr-init -> isolation -> /fr-goal -> merge request, with nothing assumed into existence off-screen. Three consequences. (1) The contract beat happens TWICE -- fr-init interviews, then /fr-goal batches its own questions. 'You are asked once' was never quite true; 'once to set the repo up, ever, and once per feature after that' is true and still small enough to be the selling point. (2) backend: gitlab is now declared on camera via fr init scaffold --backend gitlab, which both installs a versioned glab in the container and records the key detect_backend reads -- the exact key whose absence made fr silently assume GitHub before #487. (3) Beat 6 changes character: it is no longer a quickstart demonstration, since she has just watched one, so it becomes the room running fr-init on their own repos. That is the only beat that actually starts the walk-out action. Budget moves to 51-59 min, beat 3 to 30-34. Also decided: the devcontainer build and first Maven resolve are SHOWN as waits, compressed hard with the compression labelled on screen -- pre-building and cutting to a warm container would hide the biggest cost of adoption. Pre-pulling base image layers is legitimate and unlabelled.

<!-- fr:journal kind=discovery scope=spec id=v2-frgoal-is-not-fr-apply created=2026-09-19T22:59:38 -->
### v2-frgoal-is-not-fr-apply · discovery · The demo run is NOT the outstanding fr apply exercise — I conflated two GitLab surfaces

Correcting a claim I made earlier this session: that creating the demo issue and running /fr-goal on it would be the remaining end-to-end fr apply verification. Wrong twice over. The operator has since dropped the issue entirely -- invocation is /fr-goal <text> with the brief inline, which also matches what the listener will type, since she has a task in her head and not a ticket. And more fundamentally, a /fr-goal run emits a spec, a plan and a merge request; it never renders, labels or diffs tracker issues, because that is the dispatch path (fr apply --to <runner>), a different feature. So the recording proves the MR half of GitLab live on the audience's own forge, and the issue-tracking half was already proven by PR #487's own live walk -- which is what moved multibackend-gitlab-tracking to skipped with a live-verified note. Nothing outstanding; the two halves just must not be conflated.

<!-- fr:journal kind=finding scope=spec id=v2-opencode-parity-costs created=2026-09-19T23:07:19 state=open -->
### v2-opencode-parity-costs · finding [open] · The confirmed harness removes the Extensibility beat and makes the contract beat advisory

Harness confirmed as opencode --auto with github-copilot/gpt-5.6-terra, default effort, same as bc88. Read fr harness parity BEFORE recording rather than after, and two rows change the plan. (1) subagent-dispatch/opencode is ABSENT: no isolation-argument dispatch primitive exists, so fr-goal section 5's documented fallback runs phases inline. Correct behaviour, not a gap. But phase executors are never dispatched on camera, and with phases inline in one session model tiers per workload complexity have nothing to show either -- so the Extensibility angle surfaces ZERO times in the run, not once. This retroactively justifies the verbal coda: it is now the only option, and the coda must say 'on this harness phases run inline' rather than implying executors fan out. It is also WHY the run fits the budget -- 0 subagents in 17 min versus Claude's 14 in 56. (2) operator-gate/opencode is ADVISORY: no operator-question tool exists, so nothing mechanically enforces the batched-Q&A gate. It fired correctly in bc88 arm G (4 questions, capped, recommended-first, turn ended) and failed to fire AT ALL in the earlier arm A, the measured instance of gh-436 instance 2. Both question gates are affected now that fr-init is in the recording. Mitigation: this is a recording, so a bad take is re-recorded -- but only if the take is CHECKED, so the gate checks are promoted from post-run harvest to take-acceptance criteria in the runbook. Stays open until a take passes them.

<!-- fr:journal kind=finding scope=spec id=v2-opencode-parity-costs-resolved created=2026-09-19T23:17:04 state=refuted resolves=v2-opencode-parity-costs -->
### v2-opencode-parity-costs-resolved · finding [refuted] · resolves v2-opencode-parity-costs: The confirmed harness removes the Extensibility beat and makes the contract beat advisory

Half refuted by the operator, who asked why OpenCode subagents 'were not implemented'. The challenge holds and my account was wrong twice. (1) I attributed arm A's 14 sessions / 13 subagents to Claude. Both runs were OpenCode -- the #429 experiment's runbook puts every arm on opencode --auto, and arm A's 13 child sessions carry parent_id and agent=general. OpenCode HAS a dispatch primitive: Task tool plus @mention over built-in general/explore/scout. fr-goal SKILL.md:91's 'OpenCode has no dispatch primitive of its own' is false as written. (2) The parity scope_note blames the lack of an isolation-argument dispatch primitive, which cannot be the blocker: fr-isolation-required.md:149 names Hermes' delegate_task and OpenCode TOGETHER as lacking one, yet Hermes is declared enforced; and the fr-phase-executor carve-out FORBIDS an isolation argument for phase executors, so the surface requires its absence. Filed as gh-493. The narrower real limitation is that custom NAMED subagents may not be invocable on OpenCode (upstream anomalyco/opencode#29616) and super-fr ships no OpenCode agent definition to test it. Measured cost of the capability: arm A's 13 subagents cost 7.59 USD against roughly 1 USD inline, about 7x -- though arm A was also FASTER in wall clock, 56.2 min against 77.6 and 105.2 for the single-session arms, so it is a cost-versus-speed-and-isolation tradeoff rather than a plain loss. NOT refuted, and unchanged for the recording: operator-gate/opencode is advisory, both question gates can silently not fire, and the take-acceptance criteria stand. The practical plan is also unchanged, because 4.5.x's SHIPPED behaviour is inline regardless of how the declaration is worded.

<!-- fr:journal kind=discovery scope=spec id=v2-opencode-custom-subagents-work created=2026-09-19T23:26:35 -->
### v2-opencode-custom-subagents-work · discovery · Custom subagents work on OpenCode 1.18.31 — definition AND dispatch, verified live

Operator proposed adding agent keys for OpenCode and invoking with @, believing subagents should reach more harnesses than Claude. Tested rather than assumed, and it works. Definitions are read in all three documented forms -- opencode.json 'agent' key, .opencode/agents/*.md, and .opencode/agent/*.md -- all three appeared in 'opencode agent list' as (subagent). Dispatch works too: 'opencode run' with a task-tool delegation returned the subagent's reply, and the session table shows a genuine CHILD session with parent_id set, its own cost (0.0239) and tokens (9510/8), and the CUSTOM agent name in the agent column. Structurally identical to arm A's 13 @general rows but with a named custom agent. Upstream anomalyco/opencode#29616, which I cited in gh-493 as a narrower real limitation, is STALE -- filed 2026-05-27 and fixed by the installed 1.18.31. So my caveat in that issue was wrong too, and I have corrected it there with the transcript. Consequence: subagent-dispatch/opencode: absent is simply wrong; both halves of the surface work. OpenCode agents also accept a model: field, so per-phase model tiering -- what fr models exists for -- is reachable, not only context isolation. Implementation is small and rides sync-opencode.py's existing mirroring, but needs a frontmatter translation (name -> filename, tools -> permission, add mode: subagent) AND a correction to fr-goal SKILL.md:91, without which the skill still instructs inline execution however many agents are defined.

<!-- fr:journal kind=decision scope=spec id=v2-await-opencode-subagents created=2026-09-19T23:31:04 -->
### v2-await-opencode-subagents · decision · Recording waits for gh-494: subagent-enabled super-fr on OpenCode

Operator will implement gh-494 with another agent, then we record with subagents enabled. Filed gh-494 as the implementation issue, separate from gh-493 which is the diagnosis of why the parity row is wrong. Consequence for the deck, to be applied once it lands: the Extensibility beat RETURNS to the spine -- phase executors dispatching visibly, and per-phase model tiering via the OpenCode agent's model: field, which is what fr models exists for. The struck spine row should be restored and the coda's 'on this harness phases run inline' caveat removed. Two things to re-check at that point rather than assume. (1) RUNTIME: arm A's 13 subagents took 56.2 min root and cost 7.59 USD against roughly 1 USD inline. The recording budget is 15-25 min, so the run must be re-timed before committing the composition -- a dispatched run may not fit, and the remedy would be fewer phases rather than a longer talk. (2) The take-acceptance criteria gain an item: at least one session row with parent_id set AND agent = fr-phase-executor, which is also gh-494's own acceptance. Everything else in the plan is unaffected; operator-gate/opencode stays advisory and remains the highest-risk moment in the take.

<!-- fr:journal kind=discovery scope=spec id=v2-gh494-verified-beat-restored created=2026-09-20T08:04:45 -->
### v2-gh494-verified-beat-restored · discovery · gh-494 verified live; the Extensibility beat is back, but the tiering is nominal until bindings differ

Verified against the installed binary rather than the PR narrative. All four shipped agents (fr-phase-executor plus -hard/-mechanical/-standard) are installed under ~/.config/opencode/agent and visible to 'opencode agent list'. A task-tool dispatch to fr-phase-executor-hard produced a genuine child session: parented, agent=fr-phase-executor-hard, own cost 0.0253 and tokens 10098/8 -- real context isolation, not role-play. fr harness parity --check agrees and the row is now enforced on all three harnesses; sync-opencode.py --check is clean; the new tripwires pass (11). The prose that would otherwise have silently defeated the feature was also fixed -- SKILL.md:92 now instructs OpenCode to dispatch via subagent_type: fr-phase-executor-<tier>, and notes that the call carries no model so the agent NAME is the only place a tier can live, which is why tier variants exist rather than one agent with a model argument. TWO THINGS OWED BEFORE RECORDING. (1) Tiering is nominal: install.sh injects each tier's model from fr models, and on this machine all three tiers resolve to the same model, so the three agents differ by name only. Filming that proves the opposite of the point. Differentiate the bindings -- claude-haiku-4.5 or gemini-3.8-flash for mechanical against gpt-5.6-terra or claude-opus-5 for hard -- and re-run install.sh. (2) Re-time the run: dispatched phases cost and take more than inline (measured subagent arm 7.59 USD and 56.2 min against roughly 1 USD inline) and the budget is 15-25 min. Tiering mechanical phases onto a cheap model may offset this, but it is unmeasured -- a reason to measure, not a claim. If it will not fit, shorten the brief rather than the talk.
