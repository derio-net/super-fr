# Journal: 2026-09-21-fr-triage

<!-- fr:journal kind=decision scope=spec id=d1-state-home created=2026-09-21T18:59:46 -->
### d1-state-home · decision · Triage state lives in a per-scope local cache, never committed

Operator chose the local cache: ~/.cache/fr/triage/<scope>/ with scope derio-net--super-fr (repo) or derio-net (org), mirroring the <org>--<repo> marketplace convention. Consequences: triage state is not a sixth artifact kind, so no registry entry, stamp, migration or structure validator; org scope has a natural home; triaging an employer or customer org cannot leak into a public repo (third-party-privacy). A --dir flag overrides the location for anyone who wants it in git. Rejected: committed under docs/superpowers/triage/ — reviewable, but a new registered artifact kind and no home for org scope.

<!-- fr:journal kind=decision scope=spec id=d2-forge-scope created=2026-09-21T18:59:46 -->
### d2-forge-scope · decision · GitHub only for v1, every forge call behind one seam

Operator chose GitHub-only v1: new gh.list_issues and gh.list_prs wrappers beside the existing gh.list_repos(owner=), with every forge call confined to fr/triage/collect.py so glab/tea support is additive. A follow-up issue is filed for forge-neutrality. Rejected: adding listing verbs to the GhClient protocol and all three adapters now — idiomatic after gh-486/487 but roughly doubles collect, and glab/tea have no org-enumeration wired today.

<!-- fr:journal kind=decision scope=spec id=d3-status-derived created=2026-09-21T18:59:46 -->
### d3-status-derived · decision · Stages are derived from GitHub; notes live in the judgement YAML; the page is read-only

Operator chose derived status. Stage is computed from collected facts on every collect (open, PR draft, PR ready, merged, closed); notes are a field in the judgement YAML the operator or agent edits; the rendered page is read-only and re-rendered to update. One source of truth the agent can read. Rejected: in-page dropdowns persisted to browser localStorage — edits would live only in one browser, invisible to the agent and to fr triage, two sources of truth that silently diverge. This is a deliberate difference from the Artifact board, which had a shared server-side store a local file cannot have.

<!-- fr:journal kind=decision scope=spec id=d4-test-plan created=2026-09-21T18:59:47 -->
### d4-test-plan · decision · Post-merge Test Plan walks Claude Code and OpenCode live; Hermes recorded not-implemented

Operator chose a live post-merge walk on Claude Code and OpenCode, operator-driven, optionally riding along with the OpenCode verification run already in progress. CI proves the engine and that the skill prose passes the harness-neutrality scanner; only a live walk proves an agent on a given harness can follow it. The Hermes acceptance row is recorded not-implemented with that reason.

<!-- fr:journal kind=decision scope=spec id=d5-architecture created=2026-09-21T18:59:47 -->
### d5-architecture · decision · Engine is an fr CLI verb; the skill is thin prose over it (agent-decided, verified)

Not an operator question — decided by verified facts. scripts/sync-opencode.py and scripts/sync-hermes.py mirror ONLY SKILL.md (canonical glob */SKILL.md, write dest_dir/SKILL.md), so any script or template bundled in the skill directory never reaches OpenCode or Hermes. The only artifact present on every harness is the fr wheel, so the deterministic engine is fr triage {collect,check,render} and fr-triage is thin prose over it, matching every existing fr-* skill (all are SKILL.md only). Judgement is data the agent writes, never an LLM call made by the engine, per the no-claude-p-batch engine/transport split.
