schema_version: 2
phase:
  number: 1
  title: Desync observability metric (repo_desync_total)
  tag: agentic
  depends_on: []
  tracking_issue: null
tasks:
- number: 1
  title: TDD MetricsPusher.push_repo_desync_total
  steps:
  - id: P1.T1.S1
    text: |-
      RED. Add failing tests to tests/unit/test_bridge_metrics.py:
      - test_push_repo_desync_total_emits_counter_with_repo_label:
          push = vk_metrics(); push.push_repo_desync_total(repo="derio-net/runs-fr")
          body = _bodies(fake_pushgateway)
          assert "willikins_vk_bridge_repo_desync_total" in body
          assert 'repo="derio-net/runs-fr"' in body
          assert "# TYPE willikins_vk_bridge_repo_desync_total counter" in body
      - test_null_metrics_desync_is_noop:
          from fr_dispatch.metrics import NullMetrics
          NullMetrics().push_repo_desync_total(repo="x/y")  # no push, no raise
          assert _bodies(fake_pushgateway) == ""
      Run: uv run pytest tests/unit/test_bridge_metrics.py -q --no-cov
      — expect AttributeError (method absent).
  - id: P1.T1.S2
    text: |-
      GREEN. In packages/fr-dispatch/src/fr_dispatch/metrics.py:
      - Add to MetricsPusher:
          def push_repo_desync_total(self, *, repo: str) -> None:
              m = f"{self.namespace}_repo_desync_total"
              _push(f'# TYPE {m} counter\n{m}{{repo="{repo}"}} 1\n', self.job)
      - Add no-op override to NullMetrics:
          def push_repo_desync_total(self, *, repo: str) -> None:  # pragma: no cover
              pass
      - Update the module docstring's metric list to mention the new counter.
      Run: uv run pytest tests/unit/test_bridge_metrics.py -q --no-cov — expect green.
state:
  steps:
    P1.T1.S1:
      state: x
      ticked_at: '2026-06-08T14:18:25+00:00'
      note: null
    P1.T1.S2:
      state: x
      ticked_at: '2026-06-08T14:18:27+00:00'
      note: null
  completion:
    at: '2026-06-08T14:18:28+00:00'
    note: null
    observed_prs: []
