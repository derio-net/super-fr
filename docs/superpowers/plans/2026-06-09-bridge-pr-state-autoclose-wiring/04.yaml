schema_version: 2
phase:
  number: 4
  title: Version bump + full verification gates
  tag: agentic
  depends_on:
  - 3
  tracking_issue: null
tasks:
- number: 1
  title: Bump version and pass all CI gates
  steps:
  - id: P4.T1.S1
    text: |-
      Bump the patch version (touches src/): uv run python scripts/bump-version.py patch
      Then the canonical CI gates (paths per ci.yml):
        uv run ruff format packages/ tests/
        uv run ruff check packages/ tests/
        uv run mypy packages/fr/src packages/fr-dispatch/src packages/fr-vk/src
        uv run pytest -q --no-cov
      All must pass; fix any findings.
  - id: P4.T1.S2
    text: |-
      Confirm version sync + CLI: uv run python scripts/bump-version.py --check
      and uv run fr --version. Both must agree on the new number.
state:
  steps:
    P4.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P4.T1.S2:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
