schema_version: 2
phase:
  number: 2
  title: Task A — down() reclaims its container image (best-effort)
  tag: agentic
  depends_on:
  - 1
  tracking_issue: null
  acceptance:
  - isolation-down-verified
tasks:
- number: 1
  title: Image capture + best-effort rmi
  steps:
  - id: P2.T1.S1
    text: |-
      RED. test_down_reclaims_image: stub `docker inspect --format {{.Image}} <id>` (stdout) to return an image id; after a clean down assert runner.argv_for('docker') contains `["docker","rmi",<image>]`. Add test_down_rmi_failure_is_non_fatal: fail the `docker rmi` (shared image) and assert down() STILL succeeds (state deleted, no raise). Add test_down_no_container_skips_rmi: empty docker ps → no rmi call. Run pytest -k "reclaims_image or rmi" → fails.
  - id: P2.T1.S2
    text: |-
      GREEN. Add _image_for(container) → `docker inspect --format {{.Image}}` (returns id or None) and _reclaim_image(image) → best-effort `docker rmi` (returncode ignored, never raises; a warn on failure). In down(): capture `image = self._image_for(container)` BEFORE `docker rm`, then after the container-gone check call `self._reclaim_image(image)` (off the verification path). Re-run → passes; full test_isolation.py green.
state:
  steps:
    P2.T1.S1:
      state: x
      ticked_at: '2026-07-05T16:07:10+00:00'
      note: null
    P2.T1.S2:
      state: x
      ticked_at: '2026-07-05T16:07:11+00:00'
      note: null
  completion:
    at: '2026-07-05T16:07:12+00:00'
    note: null
    observed_prs: []
