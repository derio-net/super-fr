schema_version: 2
phase:
  number: 4
  title: gc — dangling vsc-* image sweep
  tag: agentic
  depends_on:
  - 3
  tracking_issue: null
  acceptance:
  - isolation-gc-reconciles-merged
tasks:
- number: 1
  title: Sweep dangling devcontainer images
  steps:
  - id: P4.T1.S1
    text: |-
      RED. test_gc_sweeps_dangling_vsc_images: stub `docker images` to list two vsc-* images, one referenced by a live container (from docker ps) and one not; assert gc rmi's only the unreferenced one, best-effort (a failing rmi is logged, sweep continues). Run → fails.
  - id: P4.T1.S2
    text: |-
      GREEN. In gc(), after workspace reaping, add _sweep_dangling_images(): list `docker images --format '{{.ID}} {{.Repository}}'` filtered to vsc-*, subtract images referenced by any live container, `docker rmi` the remainder best-effort. Re-run → passes.
state:
  steps:
    P4.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P4.T1.S2:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
