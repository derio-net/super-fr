schema_version: 2
phase:
  number: 2
  title: Bridge env VK_BRIDGE_* fallback retired (fr_vk)
  tag: agentic
  depends_on: []
  tracking_issue: null
  acceptance:
  - vk-legacy-fallbacks-removed
tasks:
- number: 1
  title: bridge_env reads only FR_BRIDGE_*
  steps:
  - id: P2.T1.S1
    text: |-
      RED. In tests/unit/test_bridge_cli.py rewrite the VK_BRIDGE cases:
      FR_BRIDGE_* still wins; a VK_BRIDGE_*-only environment yields
      bridge_env(name) is None with "legacy" not in stderr (the fallback is
      gone, not merely un-preferred). Keep the neither-set → None case.
      Run: uv run pytest tests/unit/test_bridge_cli.py -q --no-cov -k bridge_env → red.
  - id: P2.T1.S2
    text: |-
      GREEN. In packages/fr-vk/src/fr_vk/config.py::bridge_env return
      os.environ.get(f"FR_BRIDGE_{name}") only — drop the VK_BRIDGE_ read,
      its warning, and the `import sys`; update the docstring. Run the same
      -k bridge_env selection → green.
  - id: P2.T1.S3
    text: |-
      REFACTOR. Drop the stale "(legacy: VK_BRIDGE_*)" notes from the
      managed_repos / checkout-dir docstrings in
      packages/fr-vk/src/fr_vk/bridge_cli.py. Run:
      uv run ruff check packages/fr-vk && uv run pytest tests/unit/test_bridge_cli.py -q --no-cov → green.
state:
  steps:
    P2.T1.S1:
      state: x
      ticked_at: '2026-07-24T19:38:44+00:00'
      note: null
    P2.T1.S2:
      state: x
      ticked_at: '2026-07-24T19:38:44+00:00'
      note: null
    P2.T1.S3:
      state: x
      ticked_at: '2026-07-24T19:38:33+00:00'
      note: null
  completion:
    at: '2026-07-24T19:38:48+00:00'
    note: bridge_env reads FR_BRIDGE_* only; VK_BRIDGE_* fallback+warning removed;
      bridge_cli legacy docstring notes dropped. Also migrated 12 integration/unit
      test fixtures (VK_BRIDGE_REPOS/LOCK_PATH/RECOVER_ORPHAN_CARDS) to FR_BRIDGE_*
      — plan gap, fixtures fed the removed fallback.
    observed_prs: []
