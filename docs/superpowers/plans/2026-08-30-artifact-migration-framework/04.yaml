schema_version: 2
phase:
  number: 4
  title: Atomic commit — only the migrated paths
  tag: agentic
  depends_on:
  - 3
  tracking_issue: null
  acceptance:
  - migration-commit-is-path-scoped
tasks:
- number: 1
  title: RED → GREEN — path-scoped staging
  steps:
  - id: P4.T1.S1
    text: |-
      RED: create tests/unit/test_migration_commit.py against a real temp git
      repo. Assert: the commit contains ONLY the paths the migration rewrote;
      an unrelated modified file is still modified and still UNCOMMITTED
      afterwards; an unrelated STAGED file is not swept into the commit; not a
      git repo → migrate without committing, no crash; nothing changed → no
      empty commit. The unrelated-file assertions are the point — a fixture
      with a clean tree would pass against `git add -A`.
      Run: uv run pytest tests/unit/test_migration_commit.py -q --no-cov
      Expected: failures.
  - id: P4.T1.S2
    text: |-
      GREEN: implement path-scoped staging (`git add -- <paths>`), never `-A`,
      never a stash cycle. Generate a commit message naming the kinds and the
      version transition.
      Run: uv run pytest tests/unit/test_migration_commit.py -q --no-cov
      Expected: passed.
state:
  steps:
    P4.T1.S1:
      state: x
      ticked_at: '2026-08-30T14:15:18+00:00'
      note: null
    P4.T1.S2:
      state: x
      ticked_at: '2026-08-30T14:24:43+00:00'
      note: null
  completion:
    at: '2026-08-30T14:25:00+00:00'
    note: 'Path-scoped commit: fr.artifacts.commit. git add -- <paths> then git commit
      -m <msg> -- <paths>. 12 unit tests, both mutations verified.'
    observed_prs: []
