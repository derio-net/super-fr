schema_version: 2
phase:
  number: 4
  title: 'The join, end to end: a phases-file tier reaching the dispatch brief'
  tag: agentic
  depends_on:
  - 1
  - 3
  tracking_issue: null
  acceptance:
  - phase-tier-reaches-the-dispatch-brief
  tier: hard
tasks:
- number: 1
  title: One integration test spanning ingestion, plan, run and brief
  steps:
  - id: P4.T1.S1
    text: |
      RED. Add `test_a_phases_file_tier_reaches_the_dispatch_brief` to
      `tests/integration/test_fr_goal_shape.py` (which already drives
      `fr run advance` and asserts on member briefs — see its `brief["item"]`
      assertion around line 392, and reuse its harness rather than building a
      second one).

      The test walks the WHOLE chain in one go, which is the point — every link
      was independently covered and the chain was still broken:
      (1) `fr plan create --phases-file` with per-phase tiers (`hard`, then one
      phase with none); (2) record the plan on the run and advance to the
      `implement` group; (3) assert the first member brief's `resolved_tier`
      equals the tier the PHASES FILE declared — not a tier written into
      `01.yaml` by the test. Scaffolding through the CLI is load-bearing: a test
      that hand-writes the phase yaml would pass against the very bug this
      closes; (4) advance to the untiered phase's member and assert
      `resolved_tier is None`.

      Write it FIRST against the current tree and record the failure. If it passes
      before phases 1 and 3 land, the test is not exercising the CLI ingestion path
      and must be rewritten — a test that asserts the code calls the function it
      calls is the defect class this repo names explicitly.
  - id: P4.T1.S2
    text: |
      GREEN. No production change should be needed — phases 1 and 3 supply it.
      If one IS needed, that is a real finding: journal it
      (`fr journal add --scope plan --kind finding`) rather than quietly widening
      an earlier phase, because it means the unit-level coverage of phase 1 or 3
      did not pin what it claimed to.

      Run: `uv run pytest tests/integration/test_fr_goal_shape.py -q --no-cov`.
  - id: P4.T1.S3
    text: |
      REFACTOR + full gate. Fold any fixture duplication between the new test and
      its neighbours into the module's existing helpers.

      Then the complete CI gate locally, since this is the last code phase:
      `uv run ruff format packages/ tests/`,
      `uv run ruff check packages/ tests/`,
      `uv run mypy packages/fr/src packages/fr-dispatch/src packages/fr-vk/src packages/fr-cncd/src`,
      `uv run pytest` (full, WITH coverage — CI gates cov-fail-under=75),
      `uv run fr validate artifacts`.
state:
  steps:
    P4.T1.S1:
      state: x
      ticked_at: '2026-09-20T12:31:53+00:00'
      note: 'RED (demonstrated, not observed): added test_a_phases_file_tier_reaches_the_dispatch_brief
        to tests/integration/test_fr_goal_shape.py. It scaffolds the plan through
        the real ''fr plan create --phases-file'' CLI (phase 1 tier: hard + skeleton,
        phase 2 no tier), drives fr run advance to the implement group, and asserts
        the member brief''s resolved_tier. It PASSED on first run because phases 1
        and 3 have landed, so the red was demonstrated by reverting each link in turn.
        (a) Dropping tier=p.get(''tier'') from plan_cmd.py''s --phases-file loop --
        the original gh#434 bug -- fails with: AssertionError: the tier the phases
        file declared (''hard'') did not reach the implement-phase brief: {''agent'':
        ''super-fr:fr-phase-executor'', ..., ''resolved_tier'': None, ..., ''tier'':
        ''from_phase'', ...} / assert None == ''hard''. Note the brief still carries
        tier: ''from_phase'', which is exactly the half-closed shape the bug had.
        (b) Dropping the ''resolved_tier'' key from _build_member_brief fails with:
        KeyError: ''resolved_tier''. Both production files restored byte-identical
        (git status clean for packages/) and the file is 10 passed again.'
    P4.T1.S2:
      state: x
      ticked_at: '2026-09-20T12:31:58+00:00'
      note: 'GREEN. No production change was needed -- phases 1 and 3 supplied the
        whole chain, as the phase predicted. uv run pytest tests/integration/test_fr_goal_shape.py
        -q --no-cov: 10 passed in 2.20s. No finding to journal on this step.'
    P4.T1.S3:
      state: x
      ticked_at: '2026-09-20T12:36:58+00:00'
      note: 'REFACTOR + full gate. Refactor: the new test''s run-driving prelude (start
        -> brainstorm -> spec-review -> plan -> plan-review) duplicated test_grouped_goal_walks_implement_review_per_phase_to_deliver''s
        verbatim, so it was extracted as _drive_to_implement(root, run_id, branch,
        spec_rel, plan_rel) next to _walk_brief and BOTH tests now call it; _next_member_brief
        was added alongside. Dropped an initial ''# fmt: off'' experiment -- ruff
        does not honour it inside an expression and the repo uses it nowhere. Full
        CI gate, all from the worktree: ruff format -> 363 files left unchanged; ruff
        check -> All checks passed!; mypy over the four src trees -> Success: no issues
        found in 138 source files; uv run pytest (WITH coverage) -> 3 failed, 3302
        passed, 80 skipped in 203.93s, ''Required test coverage of 75% reached. Total
        coverage: 91.72%''; fr validate artifacts -> 33 artifact(s) checked -- all
        structurally valid. The 3 failures are exactly the known pre-existing trio
        (test_run_workspace.py''s two marker-refusal tests and test_workflow_check.py::test_cli_all_fails_when_nothing_is_discoverable)
        recorded in p1-preexisting-unrelated-failures -- no fourth failure, so none
        is mine.'
  completion:
    at: '2026-09-20T12:37:31+00:00'
    note: null
    observed_prs: []
