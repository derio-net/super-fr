schema_version: 2
phase:
  number: 4
  title: 'Post-merge live walk: /fr-goal on Claude Code and OpenCode, one in devcontainer
    mode (Test Plan 15)'
  tag: manual
  depends_on:
  - 3
  tracking_issue: null
  acceptance:
  - bookkeeping-turns-per-step
  - usage-devcontainer-captures
  - usage-readable-on-any-host
  - fr-goal-main-session-cost
tasks:
- number: 1
  title: Operator runs /fr-goal on a small derio-net task under each harness
  steps:
  - id: P4.T1.S1
    text: |-
      Claude Code, devcontainer mode: run /fr-goal on a small task. Check: docs/superpowers/usage/<run>.yaml
      has readable sessions (no host names in it); `fr usage report --run <run>` shows ≤ 2
      bookkeeping turns per step; any `fr isolation exec -- fr run …` attempt was refused with
      the host-side command.
  - id: P4.T1.S2
    text: |-
      OpenCode, host-worktree mode: the same run; then on a second checkout that never ran it,
      `fr run cost <run>` renders both runs. Flip usage-devcontainer-captures,
      bookkeeping-turns-per-step, usage-readable-on-any-host and fr-goal-main-session-cost with
      `fr acceptance set-status` and redacted evidence. Hermes is not walked (parity.yaml says so).
state:
  steps:
    P4.T1.S1:
      state: x
      ticked_at: '2026-09-25T23:57:03+00:00'
      note: 'Walked 2026-09-26: /fr-goal on #624, Claude Code, devcontainer mode (PR
        #635). Usage file readable (1 session, host label only, no host names); fr
        isolation exec -- fr run/usage refused exit 2 naming the host command; bookkeeping
        2.7 turns/step-item (32/12), deliver alone 4 tests= records (#607 comment).
        Subagents missing from capture (#637), no USD on Claude Code.'
    P4.T1.S2:
      state: x
      ticked_at: '2026-09-25T23:57:04+00:00'
      note: 'Walked 2026-09-26: /fr-goal on #505, OpenCode, host-worktree mode (PR
        #633). Capture recorded sessions: [] (#636); reader works given the id. fr
        run cost from a fresh clone with an empty HOME renders both runs (Claude Code
        with figures, OpenCode empty). Bookkeeping 3.6 turns/step-item (36/10). Also
        #638 (prose tests= log accepted), #639 (orphan journal), #640 (flaky test).
        Rows moved per operator: passing ones flipped, failing ones kept with evidence.'
  completion:
    at: null
    note: null
    observed_prs: []
