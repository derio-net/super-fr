schema_version: 2
phase:
  number: 1
  title: Walking skeleton — _run_network takes an optional cwd
  tag: agentic
  depends_on: []
  tracking_issue: null
  skeleton: true
  tier: mechanical
  files:
  - packages/fr/src/fr/isolation/local.py
  - tests/unit/test_isolation_network_timeouts.py
  estimate_lines: 40
tasks:
- number: 1
  title: RED/GREEN — cwd parameter
  steps:
  - id: P1.T1.S1
    text: |-
      RED: create tests/unit/test_isolation_network_timeouts.py, modelled on tests/unit/test_isolation_branch_classify.py's GitRunner (records argv + kwargs). Add a test that `LocalWorktreeDevcontainerTarget._run_network(["git","status"], cwd=<other dir>)` passes that cwd and a positive timeout to the runner, and that with no cwd it still uses repo_root. Run `uv run pytest tests/unit/test_isolation_network_timeouts.py -q --no-cov`; it fails (no cwd parameter).
  - id: P1.T1.S2
    text: |-
      GREEN: in packages/fr/src/fr/isolation/local.py change `_run_network(self, argv)` to `_run_network(self, argv, cwd: Path | None = None)`, using `cwd or self.repo_root`. Re-run the test file; it passes, as does `uv run pytest tests/unit/test_isolation_branch_classify.py -q --no-cov`.
  - id: P1.T1.S3
    text: |-
      REFACTOR: none needed beyond the one-line signature change (refactor reason: single-line parameter addition).
state:
  steps:
    P1.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P1.T1.S2:
      state: ' '
      ticked_at: null
      note: null
    P1.T1.S3:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
