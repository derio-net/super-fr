# isolation network timeouts — plan

Two phases. Phase 1 is the walking skeleton: `_run_network` gains an optional `cwd`, pinned by a first test file. Phase 2 routes the default-branch forge lookup and verify-merge's two fetches through it, moves the acceptance row, and bumps to 4.23.2. `_reap_hazard`'s fetch is out of scope (spec d5).
