schema_version: 2
phase:
  number: 1
  title: Pin distinct empty and idempotent materializer reporting cases
  tag: agentic
  depends_on: []
  tracking_issue: null
  acceptance:
  - opencode-tier-binding-reaches-dispatch
  skeleton: true
  tier: mechanical
  files:
  - tests/unit/test_opencode_agents_materialize.py
  - tests/unit/test_models_cmd.py
  estimate_lines: 80
tasks:
- number: 1
  title: Add focused reporting regressions
  steps:
  - id: P1.T1.S1
    text: |-
      RED: in tests/unit/test_opencode_agents_materialize.py assert that the materializer reports the count of discovered supported-tier agent files separately from changes, including zero when no matching files exist; ensure unrelated .md files do not count. In tests/unit/test_models_cmd.py assert the apply command reports no OpenCode agent files for an empty directory, and reports the discovered count as already up to date for correct existing agent files. Run both focused files and confirm the new assertions fail against current behavior.
state:
  steps:
    P1.T1.S1:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
