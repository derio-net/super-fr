schema_version: 2
phase:
  number: 3
  title: Version 4.23.1, acceptance rows, full verification
  tag: agentic
  depends_on:
  - 2
  tracking_issue: null
  acceptance:
  - plan-create-creates-missing-table
  - plan-create-table-errors-name-header
  - plan-rework-missing-table-stays-strict
  tier: standard
  files:
  - pyproject.toml
  - uv.lock
  - packages/*/pyproject.toml
  - plugins/*/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - packages/fr-opencode-plugin/package.json
  - docs/acceptance/**
  estimate_lines: 80
tasks:
- number: 1
  title: Bump, flip rows, verify
  steps:
  - id: P3.T1.S1
    text: |-
      Run `uv run --no-project python scripts/bump-version.py 4.23.1` (never hand-edit version files); confirm `--check` passes and `uv run fr --version` prints 4.23.1.
  - id: P3.T1.S2
    text: |-
      Flip the three matrix rows with `fr acceptance set-status --status ci --level unit=super-fr:tests/unit/test_v2_plan_ops.py --notes ...` (add test_v2_migrate.py for none); run `fr acceptance check`.
  - id: P3.T1.S3
    text: |-
      Run `uv run ruff format packages/ tests/`, `uv run ruff check packages/ tests/`, the mypy command from AGENTS.md, and the full `uv run pytest -q --no-cov -n auto` into a log file; all green.
state:
  steps:
    P3.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P3.T1.S2:
      state: ' '
      ticked_at: null
      note: null
    P3.T1.S3:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
