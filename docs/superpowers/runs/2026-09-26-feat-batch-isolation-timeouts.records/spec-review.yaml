schema_version: 1
run: 2026-09-26-feat-batch-isolation-timeouts
step: spec-review
outcome: done
journal:
  - {kind: finding, id: s1, title: "Test Plan item 2 conflates the two fetches' effect on fetched", review_scope: in, body: "Only _verdict's fetch feeds fetched; _branch_refs' is discarded. Split the case."}
  - {kind: finding, id: s2, title: "_run_network adds a hidden git config call", review_scope: in, body: "Extra local git config call precedes every _run_network call; tests must select by argv."}
  - {kind: finding, id: s3, title: "Spec omits matrix and version obligations", review_scope: in, body: "Cite the acceptance row and the 4.23.2 bump."}
  - {kind: review, id: spec-review, title: "independent spec review: 3 findings", body: "fr-spec-reviewer verified every file:line claim in the spec; raised s1-s3, all in scope, all fixed in the spec."}
resolves:
  - {id: s1, state: fixed, body: "§3.C and Test Plan 2 now separate the two fetches."}
  - {id: s2, state: fixed, body: "§3.B states the extra git config call; Test Plan 3 selects by argv."}
  - {id: s3, state: fixed, body: "New §4 Obligations names the matrix row and the 4.23.2 bump."}
evidence: {review: spec-review, reviewer: a326796a5749e4782, findings: "s1,s2,s3"}
