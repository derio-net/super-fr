schema_version: 2
run: 2026-09-26-feat-batch-usage-opencode-empty
step: spec-review
outcome: done
journal:
  - {kind: finding, id: s1, review_scope: in, title: "fr run cost never prints the unavailable reason", body: "Spec claimed cost prints the reason; cost.py:162-165 only counts. check: codebase."}
  - {kind: finding, id: s2, review_scope: in, title: "_merge could leave placeholder beside carried-forward real sessions", body: "Placeholder must be built after merge, only when merged is empty. check: consistency."}
  - {kind: finding, id: s3, review_scope: in, title: "session '' key collision in effective_entries stated harmless without proof", body: "No dollar error but counts as unavailable; needs a two-host test. check: codebase."}
  - {kind: finding, id: s4, review_scope: in, title: "needs_capture interaction and reason vocabulary unstated", body: "Clarify placeholder-only capture makes needs_capture False; new reason is a vocabulary member. check: consistency."}
  - {kind: review, id: spec-review, title: "independent spec review: 4 findings", body: "fr-spec-reviewer raised s1-s4, all in scope; code references otherwise verified (capture.py, file.py, cost.py, structure.py, readers)."}
resolves:
  - {id: s1, state: fixed, body: "Spec 3.4/Test Plan 2/goal reworded: cost shows count, reason lives in file; surfacing reason is a non-goal."}
  - {id: s2, state: fixed, body: "Spec 3.2 rewritten: placeholder built after merge only if merged list empty; test required."}
  - {id: s3, state: fixed, body: "Spec section 5 states the collision honestly and pins it with a two-host test."}
  - {id: s4, state: fixed, body: "Spec 3.4 states needs_capture behaviour and structure validator round-trip."}
evidence: {review: spec-review, reviewer: a368f6f124e0614f3}
