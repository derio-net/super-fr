schema_version: 2
run: 2026-09-26-feat-batch-verify-merge-rewrites
step: review-phase
item: phase/1
outcome: done
journal:
  - {kind: finding, id: r1, review_scope: in, title: "unresolvable-ref test passes on the old code too", body: "test_verify_merge_unresolvable_ref_raises_naming_the_branch matched a branch name the old merge-base error also carried; assert _branch_refs' own wording."}
  - {kind: finding, id: r2, review_scope: in, title: "two verify_merge tests are guards, not red-before-fix", body: "Unpushed-local and deleted-remote tests were probably green before; they guard the fail-safe."}
  - {kind: finding, id: r3, review_scope: out, title: "A reverted merge still passes the blob fallback", body: "Design-inherent to spec §A (positive evidence from base history); verify-merge still needs PR MERGED; reap keeps the branch ref."}
  - {kind: finding, id: r4, review_scope: out, title: "diff --name-only without -z quotes special paths", body: "Pre-existing; fails safe."}
  - {kind: finding, id: r5, review_scope: out, title: "One rev-parse per commit touching the path", body: "Spec accepts cost bounded to one path; early exit already present."}
  - {kind: finding, id: r6, review_scope: in, title: "No test for path with space/dash/glob or failed rev-list", body: "Optional; code is safe by inspection."}
  - {kind: review, id: review-phase-1, title: "independent review of phase 1: 6 findings (3 in, 3 out), no critical", body: "Reviewer a6a03ceb06e10253d. Blob fallback cannot pass unlanded content except the accepted revert case; ref set matches spec B; scaffold.py/commit.py untouched (verified by diff --stat, 0 lines)."}
resolves:
  - {id: r1, state: fixed, body: "Assertion now matches 'cannot resolve branch ref' (_branch_refs' wording)."}
  - {id: r2, state: refuted, body: "Spec Test Plan lists them as guards for the fail-safe; the PR body will not call them red-before-fix."}
  - {id: r3, state: out-of-scope, body: "Follows from spec §A, not a defect of this change; noted for the operator."}
  - {id: r4, state: out-of-scope, body: "Pre-existing behaviour, fails safe."}
  - {id: r5, state: out-of-scope, body: "Spec accepts the bounded cost; early exit already present."}
  - {id: r6, state: refuted, body: "Path is only passed after `--` and inside <rev>:<path>, never parsed as an option; pathspec magic can only widen candidates and the blob compare uses the literal path, so no false pass."}
evidence: {review: review-phase-1, reviewer: a6a03ceb06e10253d, model: claude-sonnet-5}
