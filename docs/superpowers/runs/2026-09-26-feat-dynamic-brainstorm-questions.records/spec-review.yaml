schema_version: 1
run: 2026-09-26-feat-dynamic-brainstorm-questions
step: spec-review
outcome: done
journal:
  - kind: finding
    id: s1
    review_scope: in
    title: "No channel carries the record's `questions` declaration into `_gate_provenance` on the --record path"
    body: "apply.py:855-867 passes only evidence/emitted/no_questions/reason to resolve_in_process (run_cmd.py:4166); _gate_provenance's `record` is fr.run.model.StepRecord (the cursor), not the record artifact that gains `questions`. The declaration would silently never reach the gate on the path fr-goal uses."
  - kind: finding
    id: s2
    review_scope: in
    title: "render_template hardcodes `schema_version: 1`; the record bump to 2 would break every fresh template"
    body: "template.py:54 is a literal; model.py:168-174 refuses schema_version != RECORD_SCHEMA_VERSION. After the bump every dispatched step's filled template would be refused."
  - kind: finding
    id: s3
    review_scope: in
    title: "§3.A.1 cites fr-goal/SKILL.md:46-55 but the AskUserQuestion clause is at :58"
    body: "Citation slip in the surface inventory."
  - kind: review
    id: spec-review-1
    title: "independent spec review: 3 findings"
    body: "Dispatched fr-spec-reviewer checked the spec against decisions d1-d4 and the codebase (file:line for _clears_gate, _gate_provenance, operator_answered_since, StepRecord/_SECTION_FIELDS, record kind registry, run_provenance, artifacts/__init__, every §3.A.1 prose surface). Verdict table checked exhaustively. Raised s1 (declaration not threaded to the gate), s2 (template hardcodes schema_version 1), s3 (citation slip). All in scope."
resolves:
  - id: s1
    state: fixed
    body: "§3.B now threads `questions: QuestionRounds | None` apply_record -> resolve_in_process -> _resolve_body -> _gate_provenance beside no_questions/reason, names the two StepRecord classes, and Test Plan 7 pins both paths."
  - id: s2
    state: fixed
    body: "§3.B requires render_template to write RECORD_SCHEMA_VERSION instead of the literal; Test Plan 5 pins that every fr-goal step's rendered template parses."
  - id: s3
    state: fixed
    body: "Citation widened to 46-62 naming the harness clause at :58."
evidence:
  review: spec-review-1
  reviewer: a80cd30b811b50304
