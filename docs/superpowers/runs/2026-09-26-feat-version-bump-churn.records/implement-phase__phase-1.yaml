schema_version: 1
run: 2026-09-26-feat-version-bump-churn
step: implement-phase
item: phase/1
outcome: done
ticks: [P1.T1.S1, P1.T1.S2, P1.T2.S1, P1.T2.S2, P1.T3.S1, P1.T3.S2]
refactor:
  P1.T1: "tests only; the one cleanup (a tuple-assert ruff flagged F631 and a hand-rolled frozen check) was folded into pytest.raises(FrozenInstanceError) before commit"
  P1.T2: "the glob helpers and per-file writers moved wholesale into scripts/version_surfaces.py, so nothing duplicated remained in bump-version.py; the label formatting was collapsed to one regex in P1.T3.S2"
journal:
  - kind: decision
    id: p1-surface-locators
    title: Surface locators are project.version, version, plugins[i].version, package[<name>].version
    phase: 1
    body: |
      scripts/version_surfaces.py returns frozen Surface(file, locator, value); file is repo-relative POSIX.
      uv.lock members are [[package]] blocks whose source has an editable or virtual key (6 here, incl.
      super-fr-workspace via editable "."); fr-opencode-plugin is not a uv member, so it has no lock line.
      Optional surfaces (marketplace.json, opencode package.json, uv.lock) are skipped when absent so the
      module works on tmp repos; the root pyproject is required. write_version rewrites uv.lock textually
      per [[package]] block (member version line only) and preserves JSON indent (2 for package.json, 4 else).
  - kind: discovery
    id: p1-write-version-byte-stable
    title: write_version is byte-stable at the current version and moves exactly 17 lines on a bump
    phase: 1
    body: |
      Checked in a scratch git copy of the tracked manifests + uv.lock: writing 4.23.0 left the tree clean;
      writing 9.9.9 changed 11 files / 17 lines (6 uv.lock member lines), no registry package touched.
      bump-version.py --check now lists 17 surfaces (was 11) and stays green.
resolves: []
evidence: {}
