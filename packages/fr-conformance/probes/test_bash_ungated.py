"""Probe: bash writes base-clone file (pins documented gap)."""

import pytest


def test_bash_ungated_placeholder():
    """Placeholder - documents that bash is currently ungated."""
    pytest.skip("Documents current gap - bash is ungated per parity.yaml - see issue #563")
