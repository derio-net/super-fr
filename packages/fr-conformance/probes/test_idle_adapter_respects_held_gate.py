"""Probe: idle adapter respects held operator gate."""

import pytest


def test_idle_adapter_respects_held_gate_placeholder():
    """Placeholder - requires live OpenCode session with held gate."""
    pytest.skip("Full probe requires live OpenCode session with held operator gate - see issue #563")
