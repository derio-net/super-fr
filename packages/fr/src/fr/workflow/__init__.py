"""Workflow shape manifests — the shape axis (spec §4.A, Phase 6+).

`fr.workflow.model`   — schema + parser (`parse_manifest`).
`fr.workflow.resolve` — repo > shipped resolution (`resolve_workflow`).
`fr.workflow.check`   — semantic validation (`check_workflow`, `fr workflow check`).
"""

from __future__ import annotations
