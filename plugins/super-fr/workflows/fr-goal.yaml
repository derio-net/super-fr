# STUB — Phase 6 of docs/superpowers/plans/2026-08-14-workflow-shapes-and-workitem-dispatch
# landed this only to give the CI tripwire (tests/unit/test_tripwire_shipped_workflows.py)
# a real, non-vacuous manifest to check. Phase 11 replaces this with the full spec §4.A
# example (isolate → brainstorm → spec-review → plan → plan-review → implement → review →
# deliver) and wires `fr-goal` itself onto it. Until then this manifest is not consumed by
# any runner — `fr run` does not exist yet (that is Phase 7's `docs/superpowers/runs/`
# cursor) — it exists purely so `fr workflow check fr-goal` has something real to validate.
workflow: fr-goal
schema: 1
description: >-
  STUB (Phase 6) — fleshed out into the full TDD feature-delivery pipeline in Phase 11.
unit: run
requires: [git, scm]
steps:
  - id: isolate
    kind: cli
    run: fr isolation up --branch {{ run.branch }}
